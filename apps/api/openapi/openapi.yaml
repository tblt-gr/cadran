openapi: 3.1.0
info:
  title: Cadran Budget API
  version: 0.1.0
  description: Versioned HTTP contract for the Cadran Budget modular monolith.
  license:
    name: MIT
    identifier: MIT
servers:
  - url: /
paths:
  /api/v1/status:
    get:
      operationId: getFoundationStatus
      summary: Check that the API foundation is available
      tags:
        - Foundation
      # The foundation probe is deliberately public; it exposes no workspace data.
      security: []
      responses:
        '200':
          description: The API foundation is ready.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FoundationStatus'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/session:
    get:
      operationId: getSession
      summary: Describe the current session
      description: >-
        Public. Reports whether the request carries an authenticated, active
        session and whether first-run password setup is still pending. Also
        plants the `csrf_token` cookie when the request lacks a valid one.
      tags:
        - Session
      security: []
      responses:
        '200':
          description: The session state for the calling browser.
          headers:
            Set-Cookie:
              description: >-
                `csrf_token=<signed token>; Path=/; SameSite=Strict` (plus
                `Secure` over HTTPS). Echo it in `X-CSRF-TOKEN` on every mutation.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SessionState'
        '500':
          $ref: '#/components/responses/InternalServerError'
    post:
      operationId: openSession
      summary: Open a session with email and password
      description: >-
        A same-origin browser mutation: it must send the X-CSRF-TOKEN header
        matching the csrf_token cookie from a prior safe response, and its
        Origin must be this server. Failed attempts are rate-limited per email
        and source address.
      tags:
        - Session
      security: []
      parameters:
        - $ref: '#/components/parameters/CsrfToken'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
      responses:
        '204':
          description: The session cookie is set. Fetch GET /api/v1/session for details.
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/InvalidCredentials'
        '403':
          $ref: '#/components/responses/Forbidden'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '500':
          $ref: '#/components/responses/InternalServerError'
    delete:
      operationId: closeSession
      summary: Close the current session
      description: >-
        A same-origin browser mutation: it must send the X-CSRF-TOKEN header
        matching the csrf_token cookie, with an Origin of this server.
      tags:
        - Session
      security: []
      parameters:
        - $ref: '#/components/parameters/CsrfToken'
      responses:
        '204':
          description: The session is invalidated. A no-op when already anonymous.
        '403':
          $ref: '#/components/responses/CsrfFailure'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/session/password:
    put:
      operationId: defineInitialPassword
      summary: Define the owner password on first run
      description: >-
        Public and one-time. Accepted only while the provisioned owner has no
        password; afterwards it answers 409. A same-origin browser mutation: it
        must send the X-CSRF-TOKEN header matching the csrf_token cookie, with an
        Origin of this server.
      tags:
        - Session
      security: []
      parameters:
        - $ref: '#/components/parameters/CsrfToken'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DefinePasswordRequest'
      responses:
        '204':
          description: The password is stored. The owner can now open a session.
        '400':
          $ref: '#/components/responses/BadRequest'
        '403':
          $ref: '#/components/responses/CsrfFailure'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/profile:
    get:
      operationId: getOwnerProfile
      summary: Read the signed-in owner's own account
      description: >-
        Requires an authenticated session. The account is resolved from the
        session; there is no way to ask for another one. It answers from the
        identity record alone, so the settings screen renders on an install
        that holds no account or transaction yet.
      tags:
        - Profile
      security:
        - sessionCookie: []
      responses:
        '200':
          description: The owner account as its own owner sees it.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OwnerProfile'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/WorkspaceForbidden'
        '409':
          $ref: '#/components/responses/OwnerNotProvisioned'
        '500':
          $ref: '#/components/responses/InternalServerError'
    patch:
      operationId: updateOwnerProfile
      summary: Edit the signed-in owner's display name
      description: >-
        A same-origin browser mutation: it must send the X-CSRF-TOKEN header
        matching the csrf_token cookie, with an Origin of this server. The
        display name is the only editable field; a body carrying any other key
        is refused rather than silently ignored.
      tags:
        - Profile
      security:
        - sessionCookie: []
      parameters:
        - $ref: '#/components/parameters/CsrfToken'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateProfileRequest'
      responses:
        '200':
          description: The stored account, with the new display name.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OwnerProfile'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/ProfileMutationForbidden'
        '409':
          $ref: '#/components/responses/OwnerNotProvisioned'
        '413':
          $ref: '#/components/responses/ProfilePayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '422':
          $ref: '#/components/responses/InvalidDisplayName'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/profile/password:
    put:
      operationId: changeOwnerPassword
      summary: Change the signed-in owner's password
      description: >-
        Requires the current password in the same request, verified server-side
        and rate-limited on its own budget, independently of sign-in throttling.
        On success every other session is revoked and the calling session is
        issued a new identifier, so the acting browser stays signed in and a
        cookie captured earlier stops working. A same-origin browser mutation:
        it must send the X-CSRF-TOKEN header matching the csrf_token cookie,
        with an Origin of this server.
      tags:
        - Profile
      security:
        - sessionCookie: []
      parameters:
        - $ref: '#/components/parameters/CsrfToken'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChangePasswordRequest'
      responses:
        '204':
          description: >-
            The password is replaced. The response carries a new session cookie;
            refetch GET /api/v1/session.
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/ProfileMutationForbidden'
        '409':
          $ref: '#/components/responses/OwnerNotProvisioned'
        '413':
          $ref: '#/components/responses/ProfilePayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '422':
          $ref: '#/components/responses/PasswordChangeRefused'
        '429':
          $ref: '#/components/responses/PasswordChangeThrottled'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/audit-events:
    get:
      operationId: listAuditEvents
      summary: Read the workspace audit trail
      description: >-
        Requires an authenticated session. Returns the calling workspace's
        audit events, newest first. The workspace is resolved server-side from
        the session; there is no way to ask for another one.
      tags:
        - Audit
      security:
        - sessionCookie: []
      parameters:
        - name: limit
          in: query
          required: false
          description: Page size, between 1 and 100. Defaults to 50.
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
        - name: cursor
          in: query
          required: false
          description: >-
            Opaque cursor returned as `nextCursor` by the previous page. Omit it
            to read the newest events.
          schema:
            type: string
      responses:
        '200':
          description: One page of the workspace audit trail.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditEventPage'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/WorkspaceForbidden'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/audit-events/{id}:
    get:
      operationId: readAuditEvent
      summary: Read one audit event of the calling workspace
      description: >-
        Requires an authenticated session. The workspace is resolved server-side
        from the session and combined with the identifier, so an event that
        belongs to another workspace answers 404 exactly like an unknown one.
      tags:
        - Audit
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          description: Identifier of an event of the calling workspace.
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The requested audit event.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditEvent'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/WorkspaceForbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/assets:
    get:
      operationId: listAssets
      summary: Read the system asset reference
      description: >-
        Requires an authenticated session. Returns the currencies and
        crypto-assets every financial figure is denominated in, with the
        precisions and rounding rule each one is read against. The reference is
        global and read-only: it belongs to no workspace and every session
        reads the same rows.
      tags:
        - Reference
      security:
        - sessionCookie: []
      parameters:
        - name: page
          in: query
          required: false
          description: Page number, between 1 and 1000. Defaults to 1.
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 1
        - name: perPage
          in: query
          required: false
          description: Page size, between 1 and 100. Defaults to 50.
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
      responses:
        '200':
          description: One page of the asset reference.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AssetPage'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/assets/{code}:
    get:
      operationId: readAsset
      summary: Read one asset of the system reference
      description: >-
        Requires an authenticated session. An unknown code and a malformed one
        answer alike, so the endpoint keeps a single contract for "no such
        asset".
      tags:
        - Reference
      security:
        - sessionCookie: []
      parameters:
        - name: code
          in: path
          required: true
          description: Asset code, such as `EUR` or `BTC`.
          schema:
            type: string
            pattern: '^[A-Z][A-Z0-9]{1,11}$'
      responses:
        '200':
          description: The requested asset.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Asset'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/products:
    get:
      operationId: listProducts
      summary: Read the system product catalogue on a business date
      description: >-
        Requires an authenticated session. Returns the financial product models
        the application ships with — Livret A, LDDS, LEP, PEA, CTO, life
        insurance — each resolved against the business date asked for. The
        catalogue is global and read-only: it belongs to no workspace, every
        session reads the same rows, and a regulatory value changes through a
        reviewed migration rather than through a request.
      tags:
        - Catalog
      security:
        - sessionCookie: []
      parameters:
        - name: page
          in: query
          required: false
          description: Page number, between 1 and 1000. Defaults to 1.
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 1
        - name: perPage
          in: query
          required: false
          description: Page size, between 1 and 100. Defaults to 25.
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 25
        - $ref: '#/components/parameters/BusinessDate'
      responses:
        '200':
          description: One page of the product catalogue, resolved on the business date.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProductPage'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/products/{code}:
    get:
      operationId: readProduct
      summary: Read one catalogue product on a business date
      description: >-
        Requires an authenticated session. An unknown code and a malformed one
        answer alike, so the endpoint keeps a single contract for "no such
        product".
      tags:
        - Catalog
      security:
        - sessionCookie: []
      parameters:
        - name: code
          in: path
          required: true
          description: Product code, such as `FR_LIVRET_A`.
          schema:
            type: string
            pattern: '^[A-Z][A-Z0-9]*(_[A-Z0-9]+)*$'
        - $ref: '#/components/parameters/BusinessDate'
      responses:
        '200':
          description: The requested product, resolved on the business date.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Product'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/categories:
    get:
      operationId: listCategories
      summary: List the calling workspace categories
      description: >-
        Requires an authenticated session. The workspace is resolved from the
        session and cannot be selected by a query parameter. Archived rows are
        omitted unless explicitly requested.
      tags:
        - Categories
      security:
        - sessionCookie: []
      parameters:
        - name: includeArchived
          in: query
          required: false
          schema:
            type: boolean
            default: false
        - name: page
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 1
        - name: perPage
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
        - name: type
          in: query
          required: false
          description: Restrict results to income or expense categories.
          schema:
            $ref: '#/components/schemas/CategoryType'
        - name: search
          in: query
          required: false
          description: Case-insensitive contained label search, bounded to 80 characters.
          schema:
            type: string
            maxLength: 80
        - name: parentEligible
          in: query
          required: false
          description: Return only active categories below the maximum tree depth.
          schema:
            type: boolean
            default: false
      responses:
        '200':
          description: One page of categories from the calling workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CategoryPage'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/WorkspaceForbidden'
        '500':
          $ref: '#/components/responses/InternalServerError'
    post:
      operationId: createCategory
      summary: Create a category in the calling workspace
      description: >-
        Creates an income or expense category. The parent, when present, must
        be active, belong to the calling workspace and carry the same type.
        The mutation requires the same-origin CSRF token.
      tags:
        - Categories
      security:
        - sessionCookie: []
      parameters:
        - $ref: '#/components/parameters/CsrfToken'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCategoryRequest'
      responses:
        '201':
          description: The category was created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Category'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/CategoryMutationForbidden'
        '409':
          description: An active sibling already uses the normalized label.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '413':
          $ref: '#/components/responses/PayloadTooLarge'
        '422':
          description: The category type, parent, metadata or bounds are invalid.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/categories/{id}:
    put:
      operationId: updateCategory
      summary: Update an active category
      description: >-
        Replaces editable category fields using optimistic versioning. Parent
        moves and archival are separate impact-aware operations outside this
        endpoint. A category already used by history cannot change type.
      tags:
        - Categories
      security:
        - sessionCookie: []
      parameters:
        - $ref: '#/components/parameters/CsrfToken'
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateCategoryRequest'
      responses:
        '200':
          description: The updated category.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Category'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/CategoryMutationForbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: The label conflicts or the submitted version is stale.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '413':
          $ref: '#/components/responses/PayloadTooLarge'
        '422':
          description: The category fields are invalid or an immutable rule was violated.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/categories/{id}/impact:
    get:
      operationId: previewCategoryImpact
      summary: Preview what a category lifecycle operation would change
      description: >-
        Answers what moving, merging, archiving or replacing a category would
        change, without changing anything. The same assessment gates the write,
        so a confirmation screen and the operation it leads to never disagree.
        `affectedClassifications` stays null with an explicit reason while no
        transaction history exists; it is never an invented `0`.
      tags:
        - Categories
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: operation
          in: query
          required: true
          schema:
            $ref: '#/components/schemas/CategoryLifecycleOperation'
        - name: targetId
          in: query
          required: false
          description: >-
            The new parent for `MOVE` (absent means the root), and the category
            receiving the history for `MERGE` and `REPLACE`. Rejected for
            `ARCHIVE`.
          schema:
            type: string
            format: uuid
        - name: effectiveFrom
          in: query
          required: false
          description: Required for `REPLACE` and rejected for every other operation.
          schema:
            type: string
            format: date
      responses:
        '200':
          description: The impact the operation would have.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CategoryImpact'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/WorkspaceForbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/categories/{id}/move:
    post:
      operationId: moveCategory
      summary: Reparent a category and the branch below it
      description: >-
        Moves the category under another active category of the same type, or
        to the root when `parentId` is null, and rewrites the depth of every
        descendant in one transaction.
      tags:
        - Categories
      security:
        - sessionCookie: []
      parameters:
        - $ref: '#/components/parameters/CsrfToken'
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MoveCategoryRequest'
      responses:
        '200':
          description: The moved category.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Category'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/CategoryMutationForbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/CategoryVersionConflict'
        '413':
          $ref: '#/components/responses/PayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '422':
          $ref: '#/components/responses/CategoryOperationRefused'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/categories/{id}/merge:
    post:
      operationId: mergeCategory
      summary: Fold a category into another one
      description: >-
        Moves the direct children under the target, records the redirection
        that makes the history of the source read as the target's, and archives
        the source. The three happen in one transaction or not at all.
      tags:
        - Categories
      security:
        - sessionCookie: []
      parameters:
        - $ref: '#/components/parameters/CsrfToken'
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MergeCategoryRequest'
      responses:
        '200':
          description: The archived source category and its redirection.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Category'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/CategoryMutationForbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/CategoryVersionConflict'
        '413':
          $ref: '#/components/responses/PayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '422':
          $ref: '#/components/responses/CategoryOperationRefused'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/categories/{id}/archive:
    post:
      operationId: archiveCategory
      summary: Retire a category without deleting it
      description: >-
        Takes the category out of the choices offered for new classifications.
        A used category is never hard-deleted, and a category with active
        children is refused until its branch is retired first.
      tags:
        - Categories
      security:
        - sessionCookie: []
      parameters:
        - $ref: '#/components/parameters/CsrfToken'
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ArchiveCategoryRequest'
      responses:
        '200':
          description: The archived category.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Category'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/CategoryMutationForbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/CategoryVersionConflict'
        '413':
          $ref: '#/components/responses/PayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '422':
          $ref: '#/components/responses/CategoryOperationRefused'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/categories/{id}/replacement:
    post:
      operationId: replaceCategory
      summary: Replace a category by another one from a date onwards
      description: >-
        Records that classifications from `effectiveFrom` belong to the target.
        The source keeps its tree position and stays selectable for the period
        before that date: history is never silently rewritten.
      tags:
        - Categories
      security:
        - sessionCookie: []
      parameters:
        - $ref: '#/components/parameters/CsrfToken'
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReplaceCategoryRequest'
      responses:
        '200':
          description: The source category and its dated redirection.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Category'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/CategoryMutationForbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/CategoryVersionConflict'
        '413':
          $ref: '#/components/responses/PayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '422':
          $ref: '#/components/responses/CategoryOperationRefused'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/account-groups:
    get:
      operationId: listAccountGroups
      summary: List the calling workspace account groups
      description: >-
        Requires an authenticated session. The workspace is resolved from the
        session and cannot be selected by a query parameter. Archived rows are
        omitted unless explicitly requested. Published shares stay null with an
        explicit reason until valuations exist; they are never `0%`.
      tags:
        - Account groups
      security:
        - sessionCookie: []
      parameters:
        - name: includeArchived
          in: query
          required: false
          schema:
            type: boolean
            default: false
        - name: page
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 1
        - name: perPage
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
        - name: parentEligible
          in: query
          required: false
          description: Return only active groups below the maximum tree depth.
          schema:
            type: boolean
            default: false
      responses:
        '200':
          description: One page of account groups from the calling workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountGroupPage'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/WorkspaceForbidden'
        '500':
          $ref: '#/components/responses/InternalServerError'
    post:
      operationId: createAccountGroup
      summary: Create an account group in the calling workspace
      description: >-
        Creates a workspace-owned exclusive group. The parent, when present,
        must be active and belong to the calling workspace. The mutation
        requires the same-origin CSRF token.
      tags:
        - Account groups
      security:
        - sessionCookie: []
      parameters:
        - $ref: '#/components/parameters/CsrfToken'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAccountGroupRequest'
      responses:
        '201':
          description: The group was created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountGroup'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/WorkspaceForbidden'
        '409':
          description: An active sibling already uses the normalized label.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '413':
          $ref: '#/components/responses/PayloadTooLarge'
        '422':
          description: The group label, parent or bounds are invalid.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/account-groups/{id}:
    put:
      operationId: updateAccountGroup
      summary: Update an active account group
      description: >-
        Replaces editable group fields using optimistic versioning. Reparenting
        is allowed when it does not create a cycle or cross a workspace.
      tags:
        - Account groups
      security:
        - sessionCookie: []
      parameters:
        - $ref: '#/components/parameters/CsrfToken'
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateAccountGroupRequest'
      responses:
        '200':
          description: The updated group.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountGroup'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/WorkspaceForbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: The label conflicts or the submitted version is stale.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '413':
          $ref: '#/components/responses/PayloadTooLarge'
        '422':
          description: The group fields are invalid or the tree would cycle.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/account-groups/{id}/archive:
    post:
      operationId: archiveAccountGroup
      summary: Archive an unused account group
      description: >-
        Archives a group that has no children and is not referenced by an
        account. Archived groups are read-only.
      tags:
        - Account groups
      security:
        - sessionCookie: []
      parameters:
        - $ref: '#/components/parameters/CsrfToken'
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ArchiveAccountGroupRequest'
      responses:
        '200':
          description: The archived group.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountGroup'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/WorkspaceForbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: The submitted version is stale.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '413':
          $ref: '#/components/responses/PayloadTooLarge'
        '422':
          description: The group is already archived, has children or is referenced.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/accounts:
    get:
      operationId: listAccounts
      summary: List the calling workspace financial accounts
      description: >-
        Requires an authenticated session. The workspace is resolved from the
        session and cannot be selected by a query parameter. Closed and
        archived accounts are omitted unless explicitly requested.
      tags:
        - Accounts
      security:
        - sessionCookie: []
      parameters:
        - name: includeArchived
          in: query
          required: false
          schema:
            type: boolean
            default: false
        - name: includeClosed
          in: query
          required: false
          schema:
            type: boolean
            default: false
        - name: page
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 1
        - name: perPage
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
        - name: kind
          in: query
          required: false
          description: Restrict results to one account kind.
          schema:
            $ref: '#/components/schemas/AccountKind'
        - $ref: '#/components/parameters/BusinessDate'
      responses:
        '200':
          description: One page of accounts from the calling workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountPage'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/WorkspaceForbidden'
        '500':
          $ref: '#/components/responses/InternalServerError'
    post:
      operationId: createAccount
      summary: Create a financial account in the calling workspace
      description: >-
        Creates an account with its denomination, kind, lifecycle dates,
        valuation mode and inclusion policies. An optional exclusive group and
        extra tags may be set at creation; omitting the group (`null`) leaves
        the account ungrouped. The asset code must exist in the
        read-only system reference. An optional `productCode` backs the account
        with a catalogue product; the submitted kind and valuation mode are then
        checked against what that product declares. The mutation requires the
        same-origin CSRF token.
      tags:
        - Accounts
      security:
        - sessionCookie: []
      parameters:
        - $ref: '#/components/parameters/CsrfToken'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAccountRequest'
      responses:
        '201':
          description: The account was created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Account'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/AccountMutationForbidden'
        '409':
          description: >-
            An active account already uses the normalized label
            (`type: /problems/account-label-taken`).
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '413':
          $ref: '#/components/responses/PayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '422':
          description: >-
            The denomination, kind, product reference, valuation mode, masked
            identifier or lifecycle dates are invalid.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/accounts/{id}:
    put:
      operationId: updateAccount
      summary: Update an active financial account
      description: >-
        Replaces the editable account fields using optimistic versioning, and
        closes or reopens the account through `closedOn`. The denomination is
        absent from the body: changing it would reinterpret every figure already
        recorded against the account. An account already used by history cannot
        change kind or product, and an archived account is read-only.
      tags:
        - Accounts
      security:
        - sessionCookie: []
      parameters:
        - $ref: '#/components/parameters/CsrfToken'
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The updated account.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Account'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/AccountMutationForbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: >-
            Either an active account already uses the normalized label
            (`type: /problems/account-label-taken`), or the submitted version is
            stale (`type: /problems/stale-version`). The two ask for opposite
            recoveries, so a client branches on the type rather than the status.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '413':
          $ref: '#/components/responses/PayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '422':
          description: >-
            The account fields are invalid, an immutable rule was violated, or
            the account is archived and read-only
            (`type: /problems/account-archived`).
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '500':
          $ref: '#/components/responses/InternalServerError'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateAccountRequest'
  /api/v1/accounts/{id}/archive:
    post:
      operationId: archiveAccount
      summary: Archive a financial account
      description: >-
        Takes the account out of the working set without deleting it, so
        history and later valuations keep a row to point at. Archiving frees the
        account label for a new active account. An already archived account is
        rejected rather than archived twice.
      tags:
        - Accounts
      security:
        - sessionCookie: []
      parameters:
        - $ref: '#/components/parameters/CsrfToken'
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ArchiveAccountRequest'
      responses:
        '200':
          description: The archived account.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Account'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/AccountMutationForbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: 'The submitted version is stale (`type: /problems/stale-version`).'
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '413':
          $ref: '#/components/responses/PayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '422':
          description: 'The account is already archived (`type: /problems/account-archived`).'
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/accounts/{id}/valuation:
    get:
      operationId: readAccountValuation
      summary: Read the latest valid balance of an account on a business date
      description: >-
        Returns the latest active snapshot on or before the requested date,
        with its age, source and missing/stale quality. A missing valuation
        is `null` on both the source and display amounts: a zero would invent
        a figure the holder never recorded. Display rounding is applied by
        the backend from the asset's display precision; the source literal
        is kept exact.
      tags:
        - Accounts
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: '#/components/parameters/BusinessDate'
      responses:
        '200':
          description: The latest valid valuation on the requested date.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountValuation'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/WorkspaceForbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/accounts/{id}/balances:
    get:
      operationId: listAccountBalances
      summary: List the balance snapshots recorded on an account
      description: >-
        The history of observed balances, superseded rows included. What
        answers on one day is the valuation resource; this list is the trail.
      tags:
        - Accounts
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: page
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 1
        - name: perPage
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
      responses:
        '200':
          description: The snapshot history of the account, newest day first.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountBalanceSnapshotPage'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/WorkspaceForbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
    post:
      operationId: recordAccountBalance
      summary: Record a manual observed balance on an account
      description: >-
        Stores a dated manual snapshot in the account's own unit. The source
        literal is kept exactly as submitted. A second active snapshot of the
        same day and source is refused unless the current version is presented,
        in which case the previous row is superseded and kept on the trail.
        The author comes from the session.
      tags:
        - Accounts
      security:
        - sessionCookie: []
      parameters:
        - $ref: '#/components/parameters/CsrfToken'
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RecordAccountBalanceRequest'
      responses:
        '201':
          description: The snapshot as recorded.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountBalanceSnapshot'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/AccountMutationForbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: >-
            An active snapshot already exists (`type: /problems/account-balance-conflict`)
            or the submitted version is stale (`type: /problems/stale-version`).
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '413':
          $ref: '#/components/responses/PayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '422':
          description: The snapshot fields are invalid or the account is archived.
          content:
            application/problem+json:
              schema:
                oneOf:
                  - $ref: '#/components/schemas/Problem'
                  - $ref: '#/components/schemas/AmountInputProblem'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/accounts/{id}/rules:
    get:
      operationId: readAccountRules
      summary: Read the rules in force for an account on a business date
      description: >-
        Resolves the ceilings, rates and terms that apply to one account of the
        calling workspace on the business date asked for. Almost nothing here
        is stored on the account: the rules are read on that date from the
        authority the account follows, so a regulatory revision reaches every
        account at once and a past statement stays re-readable against the
        rules that applied then.

        Each entry states the same rule as every authority sees it, side by
        side and never merged. `catalog` is what the system catalogue
        publishes, reached through the account's own product reference or
        through the provenance of the workspace model it follows. `inherited`
        is what that workspace model says, and is present only when a model
        sits between the account and the catalogue. `override` is what was
        recorded against this account alone, and it carries the claim that
        explains it — who recorded it, when, and why. `effectiveLayer` names
        the one that wins, so no client re-implements the precedence.

        A ceiling always states the figure it is measured against, because the
        amount alone decides nothing: 22 950 € checked against a total balance
        and the same amount checked against deposits excluding interest give
        opposite verdicts on the same passbook. A rate always resolves to a
        bracket scale, single-bracket for the products the catalogue ships
        today, so a client reads every rate the same way. A rule the account is
        expected to carry and that no layer covers on the date is listed in
        `unavailableRuleKinds` and is never returned as zero.
      tags:
        - Accounts
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: '#/components/parameters/BusinessDate'
      responses:
        '200':
          description: The rules in force for the account on the business date.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountRules'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/WorkspaceForbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/accounts/{id}/rule-overrides:
    get:
      operationId: listAccountRuleOverrides
      summary: List every rule override ever recorded on an account
      description: >-
        Withdrawn overrides included, and whatever business date is being
        looked at. This is the history of what the account claimed, by whom and
        why; what applies on one day is answered by the account's rules
        resource instead.

        A withdrawn override stays here on purpose. Deleting it would erase the
        provenance of a figure a past statement was produced against, which is
        the one thing an override must never do.
      tags:
        - Accounts
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Every override recorded on the account, oldest period first per rule kind.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountRuleOverridePage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/WorkspaceForbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
    post:
      operationId: recordAccountRuleOverride
      summary: Record a dated rule override against one account
      description: >-
        States locally what the account inherits wrongly: a rate its branch
        negotiated, a ceiling the plan was opened under. The claim is dated,
        explained and attributed; the author comes from the session and is
        never read from the body.

        An override may only state what the authority behind the account could
        itself have stated. No rate on a product that promises none, no ceiling
        on a product that tracks no balance, and no amount in a unit the
        account is not held in. An account that follows neither a product nor a
        model inherits no rule and accepts no override at all.

        Two standing overrides of the same kind may not cover the same day. An
        overlap is refused rather than reconciled, so "the ceiling this account
        claims on 12 March" always has one answer or none.
      tags:
        - Accounts
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RecordAccountRuleOverrideRequest'
      responses:
        '201':
          description: The override as recorded.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountRuleOverride'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/WorkspaceForbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: >-
            A standing override of the same kind already covers these dates
            (`type: /problems/account-rule-override-conflict`).
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '413':
          $ref: '#/components/responses/PayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '422':
          description: >-
            The override is malformed, states something the account's authority
            could not state, or the account is archived
            (`type: /problems/account-archived`).
          content:
            application/problem+json:
              schema:
                oneOf:
                  - $ref: '#/components/schemas/Problem'
                  - $ref: '#/components/schemas/AmountInputProblem'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/accounts/{id}/rule-overrides/{overrideId}/withdraw:
    post:
      operationId: withdrawAccountRuleOverride
      summary: Withdraw a rule override
      description: >-
        Withdrawing is not ending. Ending an override is a dated fact recorded
        in its `validTo`, and the account keeps resolving against it up to that
        day for ever after. Withdrawing says the claim should not have applied
        at all: it stops answering on every date, past ones included, and the
        inherited rule of each of those dates takes over again — the dated one,
        not whatever is in force today.

        The row is kept and stays readable, so the trail still shows what was
        claimed, by whom and why. Withdrawing a second time is refused rather
        than silently accepted.
      tags:
        - Accounts
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: overrideId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmptyRequest'
      responses:
        '200':
          description: The override as withdrawn.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountRuleOverride'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/WorkspaceForbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: >-
            The override was already withdrawn
            (`type: /problems/account-rule-override-conflict`).
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '413':
          $ref: '#/components/responses/PayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '422':
          description: >-
            The body carries a field, or the account is archived
            (`type: /problems/account-archived`).
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/net-worth:
    get:
      operationId: readNetWorth
      summary: Read the explainable net worth of the calling workspace
      description: >-
        Requires an authenticated session. The workspace is resolved from the
        session and cannot be selected by a query parameter. The total is the
        sum of the signed values of the accounts included in net worth and open
        on the requested day; a liability carries the negative sign while its
        stored amount stays positive. The total is null with a reason whenever
        it cannot be produced — a missing valuation, no eligible account, or
        eligible accounts denominated in more than one asset — and is never an
        invented `0`. The delta and the change rate fail independently: a real
        movement against a zero or negative base keeps its amount and publishes
        no rate. Every contributing account and every exclusive group weight
        travels with the figure so the result can be challenged.
      tags:
        - Net worth
      security:
        - sessionCookie: []
      parameters:
        - name: asOf
          in: query
          required: false
          description: Business day the aggregate is answered for. Defaults to today.
          schema:
            type: string
            format: date
        - name: comparedTo
          in: query
          required: false
          description: >-
            Earlier business day the delta is measured against. Defaults to the
            same day one month earlier, kept inside that month.
          schema:
            type: string
            format: date
      responses:
        '200':
          description: The net worth of the calling workspace on the requested day.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NetWorth'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/WorkspaceForbidden'
        '422':
          description: >-
            The workspace holds more accounts or more groups than one
            aggregate reads (`type: /problems/net-worth-scope-too-large`).
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/net-worth/history:
    get:
      operationId: readNetWorthHistory
      summary: Read the net-worth history curve of the calling workspace
      description: >-
        Requires an authenticated session. One point per month end, ending on
        the requested day itself so the curve finishes on the headline figure.
        Each point is recomputed from the valuations that were valid on its own
        day, so a figure recorded late lands on the day it describes. A month
        that cannot be computed keeps its place with a null total and a reason,
        never a fall to zero.
      tags:
        - Net worth
      security:
        - sessionCookie: []
      parameters:
        - name: asOf
          in: query
          required: false
          description: Business day the curve ends on. Defaults to today.
          schema:
            type: string
            format: date
        - name: months
          in: query
          required: false
          description: Number of points, the final one included.
          schema:
            type: integer
            minimum: 1
            maximum: 60
            default: 12
      responses:
        '200':
          description: The net-worth curve of the calling workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NetWorthHistory'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/WorkspaceForbidden'
        '422':
          description: >-
            The workspace holds more accounts or more groups than one
            aggregate reads (`type: /problems/net-worth-scope-too-large`).
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/product-models:
    get:
      operationId: listProductModels
      summary: List the calling workspace product models
      description: >-
        Requires an authenticated session. The workspace is resolved from the
        session and cannot be selected by a query parameter. These are the
        models a workspace describes itself, beside the read-only system
        catalogue; archived models are omitted unless explicitly requested.
      tags:
        - Catalog
      security:
        - sessionCookie: []
      parameters:
        - name: includeArchived
          in: query
          required: false
          schema:
            type: boolean
            default: false
        - name: page
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 1
        - name: perPage
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
      responses:
        '200':
          description: One page of product models from the calling workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProductModelPage'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/WorkspaceForbidden'
        '500':
          $ref: '#/components/responses/InternalServerError'
    post:
      operationId: createProductModel
      summary: Describe a product model in the calling workspace
      description: >-
        Records a model the workspace describes itself: a family, an envelope, a
        yield, a valuation mode, capabilities, and its own dated rule periods.
        Nothing here reads or writes the system catalogue. A rate period is a
        bracket scale with an explicit `MARGINAL` or `FLAT_BY_BRACKET` mode; a
        single rate is the one-bracket case of the same shape. The mutation
        requires the same-origin CSRF token.
      tags:
        - Catalog
      security:
        - sessionCookie: []
      parameters:
        - $ref: '#/components/parameters/CsrfToken'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateProductModelRequest'
      responses:
        '201':
          description: The model was recorded.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProductModel'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/ProductModelMutationForbidden'
        '409':
          description: >-
            An active model already uses the name
            (`type: /problems/product-model-name-taken`).
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '413':
          $ref: '#/components/responses/ProductModelPayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '422':
          description: >-
            The name, family, envelope, yield, valuation mode, capabilities or a
            dated period is invalid. A rate scale that overlaps, leaves a gap,
            does not start at zero or leaves no open upper bracket is refused
            here and nothing is persisted.
          content:
            application/problem+json:
              schema:
                oneOf:
                  - $ref: '#/components/schemas/Problem'
                  - $ref: '#/components/schemas/AmountInputProblem'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/product-models/from-product:
    post:
      operationId: createProductModelFromProduct
      summary: Start a product model from a system catalogue product
      description: >-
        Copies a catalogue product into a workspace model. The copy is made by
        the server from the catalogue row, so a model claiming to come from a
        Livret A carries what the catalogue says a Livret A is. It takes the
        description and the dated periods with their effective dates, and drops
        every figure's publication and verification trace; from that moment the
        model no longer tracks the catalogue. The mutation requires the
        same-origin CSRF token.
      tags:
        - Catalog
      security:
        - sessionCookie: []
      parameters:
        - $ref: '#/components/parameters/CsrfToken'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateProductModelFromProductRequest'
      responses:
        '201':
          description: The model was started from the catalogue product.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProductModel'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/ProductModelMutationForbidden'
        '409':
          description: >-
            An active model already uses the name
            (`type: /problems/product-model-name-taken`).
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '413':
          $ref: '#/components/responses/ProductModelPayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '422':
          description: >-
            The name or valuation mode is invalid, or the product code does not
            exist in the system catalogue. An unknown and a malformed code
            answer alike.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/product-models/{id}:
    get:
      operationId: readProductModel
      summary: Read one product model of the calling workspace
      description: >-
        Returns the model with every dated period it ever recorded, including
        when it is archived: an account created from a model must keep resolving
        against the description it was created with. An unknown identifier and
        one that belongs to another workspace answer alike.
      tags:
        - Catalog
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The requested product model.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProductModel'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/WorkspaceForbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/product-models/{id}/duplicate:
    post:
      operationId: duplicateProductModel
      summary: Duplicate a product model of the calling workspace
      description: >-
        Copies the description and the dated periods with their effective dates
        untouched, under a new identity and name, and nothing else: no account,
        balance, valuation or external identifier has ever belonged to a model.
        The copy records the model it started from and stops following it. An
        archived model cannot be duplicated. The mutation requires the
        same-origin CSRF token.
      tags:
        - Catalog
      security:
        - sessionCookie: []
      parameters:
        - $ref: '#/components/parameters/CsrfToken'
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DuplicateProductModelRequest'
      responses:
        '201':
          description: The model was duplicated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProductModel'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/ProductModelMutationForbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: >-
            An active model already uses the name
            (`type: /problems/product-model-name-taken`).
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '413':
          $ref: '#/components/responses/ProductModelPayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '422':
          description: >-
            The name is invalid, or the source model is archived and cannot
            start a new one (`type: /problems/product-model-archived`).
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/product-models/{id}/rules:
    post:
      operationId: addProductModelRule
      summary: Record a dated period on a product model
      description: >-
        Editing a model is adding to it. Recording a period appends it and
        closes the open-ended period of the same kind it supersedes the day
        before it starts, so a past statement still resolves against the figures
        that covered it. Nothing already recorded is rewritten or removed, and
        there is no endpoint that edits or deletes a period. Optimistic
        versioning guards the write. The mutation requires the same-origin CSRF
        token.
      tags:
        - Catalog
      security:
        - sessionCookie: []
      parameters:
        - $ref: '#/components/parameters/CsrfToken'
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddProductModelRuleRequest'
      responses:
        '200':
          description: The model with the recorded period.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProductModel'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/ProductModelMutationForbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: >-
            The submitted version is stale
            (`type: /problems/stale-version`). Reload the model and reapply.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '413':
          $ref: '#/components/responses/ProductModelPayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '422':
          description: >-
            The period is invalid, contradicts a recorded period of the same
            kind, or the model is archived and read-only
            (`type: /problems/product-model-archived`).
          content:
            application/problem+json:
              schema:
                oneOf:
                  - $ref: '#/components/schemas/Problem'
                  - $ref: '#/components/schemas/AmountInputProblem'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/product-models/{id}/archive:
    post:
      operationId: archiveProductModel
      summary: Archive a product model
      description: >-
        Takes the model out of the working set and frees its name, without
        deleting it: it stays readable by identifier with everything it ever
        said, so an account created from it keeps a reproducible reference. An
        already archived model is rejected. The mutation requires the
        same-origin CSRF token.
      tags:
        - Catalog
      security:
        - sessionCookie: []
      parameters:
        - $ref: '#/components/parameters/CsrfToken'
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ArchiveProductModelRequest'
      responses:
        '200':
          description: The archived model.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProductModel'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/ProductModelMutationForbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: 'The submitted version is stale (`type: /problems/stale-version`).'
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '413':
          $ref: '#/components/responses/ProductModelPayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '422':
          description: 'The model is already archived (`type: /problems/product-model-archived`).'
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/transactions:
    get:
      operationId: listTransactions
      summary: List the calling workspace transactions
      description: >-
        Requires an authenticated session. Newest booked day first, then
        identifier. Voided rows are omitted unless explicitly requested.
        This release accepts only account, voided-inclusion, categorization,
        page size and cursor filters.
      tags:
        - Transactions
      security:
        - sessionCookie: []
      parameters:
        - name: accountId
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: includeVoided
          in: query
          required: false
          schema:
            type: boolean
            default: false
        - name: categorization
          in: query
          required: false
          description: >-
            `NONE` restricts the page to the non-voided transactions carrying
            no split at all — the "to categorise" queue — and takes precedence
            over includeVoided.
          schema:
            type: string
            enum:
              - NONE
        - name: pageSize
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
        - name: cursor
          in: query
          required: false
          description: Opaque cursor encoding the last returned booked day and identifier.
          schema:
            type: string
            minLength: 1
            maxLength: 128
      responses:
        '200':
          description: One cursor page of transactions from the calling workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TransactionPage'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/WorkspaceForbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
    post:
      operationId: createTransaction
      summary: Record a signed movement on one account
      description: >-
        Creates a pending or booked transaction on an active account of the
        calling workspace. An inflow is positive and an outflow negative.
        The submitted decimal scale is stored beside the amount. The mutation
        requires the same-origin CSRF token.
      tags:
        - Transactions
      security:
        - sessionCookie: []
      parameters:
        - $ref: '#/components/parameters/CsrfToken'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTransactionRequest'
      responses:
        '201':
          description: The transaction was created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Transaction'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/TransactionMutationForbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: A linked account or category changed concurrently.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '413':
          $ref: '#/components/responses/TransactionPayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '422':
          description: >-
            The amount, dates, nature, category or bounds are invalid, or the
            splits allocation does not sum to the amount
            (`type: /problems/splits.sum_mismatch`), carries more than twenty
            rows (`type: /problems/splits.too_many`) or repeats a category
            (`type: /problems/splits.duplicate_category`).
          content:
            application/problem+json:
              schema:
                oneOf:
                  - $ref: '#/components/schemas/Problem'
                  - $ref: '#/components/schemas/AmountInputProblem'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/transactions/{id}:
    get:
      operationId: getTransaction
      summary: Read one transaction of the calling workspace
      tags:
        - Transactions
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The transaction.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Transaction'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/WorkspaceForbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
    put:
      operationId: updateTransaction
      summary: Replace the editable fields of a transaction
      description: >-
        Optimistic versioning. The account, asset, raw label and provenance
        never change. A voided or rejected transaction cannot be edited.
      tags:
        - Transactions
      security:
        - sessionCookie: []
      parameters:
        - $ref: '#/components/parameters/CsrfToken'
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateTransactionRequest'
      responses:
        '200':
          description: The updated transaction.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Transaction'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/TransactionMutationForbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: >-
            The submitted version is stale (`type: /problems/stale-version`)
            or the transaction is terminal (`type: /problems/transaction-conflict`).
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '413':
          $ref: '#/components/responses/TransactionPayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '422':
          description: >-
            The edit is invalid, tried to change an immutable field, targets
            a transfer leg or its fee
            (`type: /problems/transaction-belongs-to-transfer`), or the
            splits allocation does not sum to the amount
            (`type: /problems/splits.sum_mismatch`), carries more than twenty
            rows (`type: /problems/splits.too_many`) or repeats a category
            (`type: /problems/splits.duplicate_category`).
          content:
            application/problem+json:
              schema:
                oneOf:
                  - $ref: '#/components/schemas/Problem'
                  - $ref: '#/components/schemas/AmountInputProblem'
                  - $ref: '#/components/schemas/TransactionBelongsToTransferProblem'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/transactions/{id}/splits:
    put:
      operationId: replaceTransactionSplits
      summary: Replace the whole split allocation of a transaction
      description: >-
        Zero to twenty splits, each sharing the transaction asset and sign,
        no category repeated, summing exactly to the transaction amount, or
        an empty list to return the transaction to the "to categorise" queue.
        The amount, state and every other field stay untouched.
        Optimistic versioning.
      tags:
        - Transactions
      security:
        - sessionCookie: []
      parameters:
        - $ref: '#/components/parameters/CsrfToken'
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReplaceTransactionSplitsRequest'
      responses:
        '200':
          description: The transaction with its new allocation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Transaction'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/TransactionMutationForbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: >-
            The submitted version is stale (`type: /problems/stale-version`)
            or the transaction is terminal (`type: /problems/transaction-conflict`).
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '413':
          $ref: '#/components/responses/TransactionPayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '422':
          description: >-
            The transaction is a transfer leg or its fee
            (`type: /problems/transaction-belongs-to-transfer`), or the
            allocation does not sum to the amount
            (`type: /problems/splits.sum_mismatch`), carries more than twenty
            rows (`type: /problems/splits.too_many`), repeats a category
            (`type: /problems/splits.duplicate_category`), or a split is
            zero, of the opposite sign, of another asset, or its category is
            archived or of the wrong type.
          content:
            application/problem+json:
              schema:
                oneOf:
                  - $ref: '#/components/schemas/Problem'
                  - $ref: '#/components/schemas/AmountInputProblem'
                  - $ref: '#/components/schemas/TransactionBelongsToTransferProblem'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/transactions/{id}/void:
    post:
      operationId: voidTransaction
      summary: Void a transaction without deleting it
      description: >-
        Sets the state to VOIDED, keeps the row, and excludes it from listings
        that do not ask for voided movements. Terminal transactions cannot be
        voided again.
      tags:
        - Transactions
      security:
        - sessionCookie: []
      parameters:
        - $ref: '#/components/parameters/CsrfToken'
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VoidTransactionRequest'
      responses:
        '200':
          description: The voided transaction.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Transaction'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/TransactionMutationForbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: >-
            The submitted version is stale (`type: /problems/stale-version`)
            or the transaction is already terminal (`type: /problems/transaction-conflict`).
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '413':
          $ref: '#/components/responses/TransactionPayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '422':
          description: >-
            The transaction is a transfer leg or its fee
            (`type: /problems/transaction-belongs-to-transfer`).
          content:
            application/problem+json:
              schema:
                oneOf:
                  - $ref: '#/components/schemas/Problem'
                  - $ref: '#/components/schemas/TransactionBelongsToTransferProblem'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/transactions/{id}/duplicate:
    post:
      operationId: duplicateTransaction
      summary: Duplicate a transaction as a booked movement dated today
      description: >-
        Copies account, nature, amount, labels, counterparty, note, payment
        method and category. Drops external identity, provenance, bank
        reference and masked card. Transfer legs and refunds cannot be duplicated.
      tags:
        - Transactions
      security:
        - sessionCookie: []
      parameters:
        - $ref: '#/components/parameters/CsrfToken'
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '201':
          description: The duplicated transaction.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Transaction'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/TransactionMutationForbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: A linked account or category changed concurrently.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '422':
          description: A transfer leg or refund cannot be duplicated.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/transfers:
    post:
      operationId: createTransfer
      summary: Move money between two accounts of the calling workspace atomically
      description: >-
        Creates both legs — source negative, target positive — and an
        optional fee, in one PostgreSQL transaction. sourceAmount and
        targetAmount are positive magnitudes; the server applies the signs.
        Same-asset transfers may omit targetAmount, defaulting it to the
        source magnitude; a cross-asset transfer must supply it, and the
        exchange rate is derived and stored on both legs. Neither leg carries
        income or expense, and neither accepts a split.
      tags:
        - Transfers
      security:
        - sessionCookie: []
      parameters:
        - $ref: '#/components/parameters/CsrfToken'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTransferRequest'
      responses:
        '201':
          description: The transfer with both legs and the optional fee.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Transfer'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/TransactionMutationForbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: A source or target account changed concurrently.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '413':
          $ref: '#/components/responses/TransactionPayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '422':
          description: >-
            The accounts, amounts, dates, state, label or fee are invalid.
            The source and target account must differ
            (`type: /problems/transfer.same_account`), and a same-asset pair
            with an explicit targetAmount must net exactly to zero
            (`type: /problems/transfer.amount_mismatch`).
          content:
            application/problem+json:
              schema:
                oneOf:
                  - $ref: '#/components/schemas/Problem'
                  - $ref: '#/components/schemas/AmountInputProblem'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/transfers/{id}:
    get:
      operationId: getTransfer
      summary: Read one transfer of the calling workspace
      tags:
        - Transfers
      security:
        - sessionCookie: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The transfer.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Transfer'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/WorkspaceForbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
    put:
      operationId: updateTransfer
      summary: Replace the editable fields of a transfer atomically
      description: >-
        Optimistic versioning. The two accounts and both leg amounts are set
        once at creation and never change; an edit corrects the state,
        dates, label, note or fee of both legs together. Voiding and
        recreating the transfer is how an amount is corrected.
      tags:
        - Transfers
      security:
        - sessionCookie: []
      parameters:
        - $ref: '#/components/parameters/CsrfToken'
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateTransferRequest'
      responses:
        '200':
          description: The updated transfer.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Transfer'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/TransactionMutationForbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: >-
            The submitted version is stale (`type: /problems/stale-version`)
            or the transfer is already voided (`type: /problems/transfer-conflict`).
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '413':
          $ref: '#/components/responses/TransactionPayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '422':
          description: The edit is invalid or tried to change an immutable field.
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/transfers/{id}/void:
    post:
      operationId: voidTransfer
      summary: Void a transfer and its legs together, without deleting them
      description: >-
        Voids both legs and the fee, if any, in one PostgreSQL transaction.
        An already-voided transfer cannot be voided again.
      tags:
        - Transfers
      security:
        - sessionCookie: []
      parameters:
        - $ref: '#/components/parameters/CsrfToken'
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VoidTransferRequest'
      responses:
        '200':
          description: The voided transfer.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Transfer'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/TransactionMutationForbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: >-
            The submitted version is stale (`type: /problems/stale-version`)
            or the transfer is already voided (`type: /problems/transfer-conflict`).
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '413':
          $ref: '#/components/responses/TransactionPayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'
components:
  securitySchemes:
    sessionCookie:
      type: apiKey
      in: cookie
      name: cadran_session
      description: >-
        Same-origin session cookie issued by POST /api/v1/session. HttpOnly, so
        the browser attaches it automatically and no script can read it.
  responses:
    Unauthorized:
      description: The request carries no authenticated session.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    InternalServerError:
      description: An unexpected server error occurred.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    UnsupportedMediaType:
      description: The endpoint requires an application/json request body.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    BadRequest:
      description: The request body or a query parameter is missing or malformed.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    InvalidCredentials:
      description: The email or password is incorrect. Identical for an unknown email.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    Forbidden:
      description: >-
        On sign-in, either the credentials were valid but the account is
        disabled (`type: about:blank`), or the CSRF / Origin check failed
        (`type: /problems/csrf-token`).
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    WorkspaceForbidden:
      description: The session is attached to no workspace, so there is nothing to read.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    NotFound:
      description: >-
        No such object in the calling workspace. Identical whether the
        identifier is unknown or belongs to another workspace.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    CsrfFailure:
      description: 'The CSRF token or Origin check failed (`type: /problems/csrf-token`).'
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    CategoryMutationForbidden:
      description: >-
        Either the CSRF / Origin check failed (`type: /problems/csrf-token`),
        or the authenticated session belongs to no workspace (`type: about:blank`).
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    CategoryOperationRefused:
      description: >-
        The impact assessment refuses the operation, or the request body is
        invalid. A refusal carries `type: /problems/category-operation-refused`
        and names every reason in `blockers`.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/CategoryOperationProblem'
    CategoryVersionConflict:
      description: The submitted version is stale or the label conflicts with an active sibling.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    AccountMutationForbidden:
      description: >-
        Either the CSRF / Origin check failed (`type: /problems/csrf-token`),
        or the authenticated session belongs to no workspace (`type: about:blank`).
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    ProductModelMutationForbidden:
      description: >-
        Either the CSRF / Origin check failed (`type: /problems/csrf-token`),
        or the authenticated session belongs to no workspace (`type: about:blank`).
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    TransactionMutationForbidden:
      description: >-
        Either the CSRF / Origin check failed (`type: /problems/csrf-token`),
        or the authenticated session belongs to no workspace (`type: about:blank`).
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    TransactionPayloadTooLarge:
      description: The transaction JSON body exceeds 32 KiB.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    PayloadTooLarge:
      description: The JSON request body exceeds the 16 KiB mutation limit.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    ProductModelPayloadTooLarge:
      description: The JSON request body exceeds the 32 KiB mutation limit.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    OwnerNotProvisioned:
      description: >-
        The account behind the session no longer exists or has lost its
        password. The browser is signed out on its next session probe.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    ProfileMutationForbidden:
      description: >-
        Either the CSRF / Origin check failed (`type: /problems/csrf-token`),
        or the authenticated session belongs to no workspace (`type: about:blank`).
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    ProfilePayloadTooLarge:
      description: The JSON request body exceeds the 4 KiB profile limit.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    InvalidDisplayName:
      description: >-
        The display name is empty, whitespace only, or longer than 100
        characters (`type: /problems/invalid-display-name`).
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    PasswordChangeRefused:
      description: >-
        The change was refused. The `type` names which field caused it:
        `/problems/invalid-current-password`, `/problems/weak-password` when the
        new password is outside 12 to 128 characters, or
        `/problems/reused-password` when it equals the current one. Nothing is
        written in any of the three cases.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    PasswordChangeThrottled:
      description: >-
        Too many current-password checks for this account. This budget is
        separate from sign-in throttling: exhausting it does not lock the owner
        out of signing in.
      headers:
        Retry-After:
          description: Seconds to wait before retrying.
          schema:
            type: integer
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    TooManyRequests:
      description: >-
        Too many failed sign-in attempts for this email and source address.
        Identical whether or not the email exists. Retry after the window.
      headers:
        Retry-After:
          description: Seconds to wait before retrying.
          schema:
            type: integer
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    Conflict:
      description: The owner password has already been defined.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    UnprocessableEntity:
      description: The password does not satisfy the account password policy.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
  parameters:
    CsrfToken:
      name: X-CSRF-TOKEN
      in: header
      required: true
      description: >-
        Double-submit CSRF token: the value of the `csrf_token` cookie from a
        prior safe response. Rejected with 403 if missing, altered, or expired.
      schema:
        type: string
    BusinessDate:
      name: asOf
      in: query
      required: false
      description: >-
        Business date the catalogue rules are resolved against, as an ISO 8601
        calendar day. Defaults to today. A rule is selected by this date and
        never by the current date, so reading a past statement shows the
        ceiling and rate that applied then.
      schema:
        type: string
        format: date
        examples:
          - '2026-09-02'
  schemas:
    FoundationStatus:
      type: object
      additionalProperties: false
      required:
        - status
        - apiVersion
      properties:
        status:
          type: string
          const: ready
        apiVersion:
          type: string
          const: v1
    SessionState:
      type: object
      additionalProperties: false
      required:
        - provisioned
        - authenticated
        - setupRequired
        - user
        - workspace
      properties:
        provisioned:
          type: boolean
          description: True once an owner account exists on this server.
        authenticated:
          type: boolean
          description: True when an active, authenticated session is attached.
        setupRequired:
          type: boolean
          description: True when the provisioned owner has no password yet.
        user:
          oneOf:
            - $ref: '#/components/schemas/SessionUser'
            - type: 'null'
        workspace:
          oneOf:
            - $ref: '#/components/schemas/SessionWorkspace'
            - type: 'null'
    SessionUser:
      type: object
      additionalProperties: false
      required:
        - id
        - email
        - displayName
      properties:
        id:
          type: string
          format: uuid
        email:
          type: string
          format: email
        displayName:
          type: string
    SessionWorkspace:
      type: object
      additionalProperties: false
      required:
        - id
        - role
      properties:
        id:
          type: string
          format: uuid
        role:
          type: string
          enum:
            - OWNER
    LoginRequest:
      type: object
      additionalProperties: false
      required:
        - email
        - password
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          format: password
    DefinePasswordRequest:
      type: object
      additionalProperties: false
      required:
        - password
      properties:
        password:
          type: string
          format: password
          minLength: 12
          maxLength: 128
    OwnerProfile:
      type: object
      additionalProperties: false
      required:
        - id
        - email
        - displayName
      properties:
        id:
          type: string
          format: uuid
        email:
          type: string
          format: email
          description: >-
            The firewall identifier. Read-only here: changing it would rewrite
            the subject of every open session.
        displayName:
          type: string
          minLength: 1
          maxLength: 100
    UpdateProfileRequest:
      type: object
      additionalProperties: false
      required:
        - displayName
      properties:
        displayName:
          type: string
          minLength: 1
          maxLength: 100
          description: >-
            Surrounding whitespace is trimmed before the length is measured, and
            the length counts Unicode code points.
    ChangePasswordRequest:
      type: object
      additionalProperties: false
      required:
        - currentPassword
        - newPassword
      properties:
        currentPassword:
          type: string
          format: password
          description: The password currently on the account, re-verified server-side.
        newPassword:
          type: string
          format: password
          minLength: 12
          maxLength: 128
          description: Must differ from the current password.
    AuditEventPage:
      type: object
      additionalProperties: false
      required:
        - items
        - nextCursor
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/AuditEvent'
        nextCursor:
          description: Cursor for the following page, or null on the last page.
          oneOf:
            - type: string
            - type: 'null'
    AuditEvent:
      type: object
      additionalProperties: false
      required:
        - id
        - occurredAt
        - actorId
        - eventType
        - entityType
        - entityId
        - before
        - after
      properties:
        id:
          type: string
          format: uuid
        occurredAt:
          type: string
          format: date-time
        actorId:
          description: >-
            The user who performed the operation, or null when it had no
            authenticated actor - console provisioning and the first-run
            password flow.
          oneOf:
            - type: string
              format: uuid
            - type: 'null'
        eventType:
          type: string
          examples:
            - session.opened
        entityType:
          type: string
          examples:
            - user
        entityId:
          type: string
          format: uuid
        before:
          $ref: '#/components/schemas/AuditDiffSide'
        after:
          $ref: '#/components/schemas/AuditDiffSide'
    AuditDiffSide:
      description: >-
        Bounded structured attributes, or null when the event has no such side.
        Sensitive attribute values are stored redacted, never in clear.
      oneOf:
        - type: object
          additionalProperties:
            oneOf:
              - type: string
              - type: 'null'
        - type: 'null'
    AssetPage:
      type: object
      additionalProperties: false
      required:
        - items
        - page
        - perPage
        - total
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Asset'
        page:
          type: integer
          minimum: 1
        perPage:
          type: integer
          minimum: 1
          maximum: 100
        total:
          description: Number of assets the reference holds.
          type: integer
          minimum: 0
    Asset:
      type: object
      additionalProperties: false
      required:
        - code
        - kind
        - displayName
        - storagePrecision
        - displayPrecision
        - roundingMode
        - displayStep
      properties:
        code:
          type: string
          pattern: '^[A-Z][A-Z0-9]{1,11}$'
          examples:
            - EUR
        kind:
          type: string
          enum:
            - FIAT
            - CRYPTO
        displayName:
          type: string
        storagePrecision:
          description: >-
            Decimal places a *submitted* figure of this asset may carry. A
            value beyond it is refused before persistence, never rounded in
            silence. It does not cap a derived figure: a conversion or a split
            keeps the full 24 decimals of the storage type.
          type: integer
          minimum: 0
          maximum: 24
        displayPrecision:
          description: >-
            Decimal places an interface or export shows. Never greater than
            `storagePrecision`, which JSON Schema cannot express here: showing
            more decimals than are stored would invent digits. A non-zero value
            smaller than the last shown decimal is displayed as such, never as
            a rounded zero.
          type: integer
          minimum: 0
          maximum: 24
        roundingMode:
          description: Rule the presentation boundary applies when it shows fewer decimals than are stored.
          type: string
          enum:
            - HALF_UP
            - HALF_EVEN
            - DOWN
        displayStep:
          $ref: '#/components/schemas/DecimalAmount'
    ProductPage:
      type: object
      additionalProperties: false
      required:
        - items
        - page
        - perPage
        - total
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Product'
        page:
          type: integer
          minimum: 1
        perPage:
          type: integer
          minimum: 1
          maximum: 100
        total:
          description: Number of products the catalogue holds.
          type: integer
          minimum: 0
    CeilingBasis:
      description: >-
        The figure a ceiling is compared against. Three of these four measures
        give a different verdict on the same account, so the amount alone
        decides nothing. A share savings plan is capped on the contributions
        paid into it, whatever the plan is worth, so a plan that grew past its
        ceiling through market value has broken no rule. A regulated passbook is
        capped on what was deposited, credited interest excluded, so a passbook
        carried past its ceiling by its own interest has broken no rule either.
        A combined ceiling is measured across the products sharing it and can be
        reached while no single account is.
      type: string
      enum:
        - NONE
        - BALANCE_EXCLUDING_INTEREST
        - TOTAL_BALANCE
        - CONTRIBUTIONS
        - COMBINED_CONTRIBUTIONS
    ProductCode:
      description: >-
        The stable identifier of a catalogue product. The catalogue is a global
        system reference with no workspace, so a code either exists for every
        workspace or for none.
      type: string
      minLength: 3
      maxLength: 32
      pattern: '^[A-Z][A-Z0-9]*(_[A-Z0-9]+)*$'
      examples:
        - FR_LIVRET_A
    Product:
      type: object
      additionalProperties: false
      required:
        - code
        - displayName
        - jurisdiction
        - accountKind
        - wrapperKind
        - yieldKind
        - yieldGuaranteed
        - ceilingBasis
        - defaultGroupCode
        - capabilities
        - catalogVersion
        - archivedAt
        - asOf
        - rules
        - unavailableRuleKinds
      properties:
        code:
          type: string
          pattern: '^[A-Z][A-Z0-9]*(_[A-Z0-9]+)*$'
          examples:
            - FR_LIVRET_A
        displayName:
          type: string
        jurisdiction:
          description: ISO 3166-1 alpha-2 country code, or null for a model that belongs to no country.
          oneOf:
            - type: string
              pattern: '^[A-Z]{2}$'
            - type: 'null'
        accountKind:
          $ref: '#/components/schemas/AccountKind'
        wrapperKind:
          $ref: '#/components/schemas/ProductWrapperKind'
        yieldKind:
          $ref: '#/components/schemas/ProductYieldKind'
        yieldGuaranteed:
          description: >-
            Whether the rate the catalogue carries is owed to the holder. False
            for every market product, so no interface has to infer a promise
            from a product name.
          type: boolean
        ceilingBasis:
          $ref: '#/components/schemas/CeilingBasis'
        defaultGroupCode:
          oneOf:
            - type: string
              pattern: '^[A-Z][A-Z0-9]*(_[A-Z0-9]+)*$'
            - type: 'null'
        capabilities:
          description: >-
            Explicit implemented behaviors enabled by this product. Consumers
            must use these values rather than infer behavior from the product
            code or display name.
          type: array
          minItems: 1
          uniqueItems: true
          items:
            $ref: '#/components/schemas/ProductCapability'
        catalogVersion:
          type: integer
          minimum: 1
        archivedAt:
          oneOf:
            - type: string
              format: date-time
            - type: 'null'
        asOf:
          description: The business date the rules below were resolved against.
          type: string
          format: date
        rules:
          description: The rules covering `asOf`, at most one per rule kind.
          type: array
          items:
            $ref: '#/components/schemas/ProductRule'
        unavailableRuleKinds:
          description: >-
            Rule kinds this product is expected to carry and that no sourced
            period covers on `asOf`. An interface shows them as unavailable
            with their reason; it never shows them as zero.
          type: array
          items:
            $ref: '#/components/schemas/ProductRuleKind'
    ProductCapability:
      type: string
      enum:
        - SUPPORTS_BALANCE
        - SUPPORTS_TRANSACTIONS
        - SUPPORTS_INTEREST
        - SUPPORTS_HOLDINGS
        - SUPPORTS_TRADES
        - SUPPORTS_ARBITRAGE
        - SUPPORTS_CONTRIBUTIONS
        - SUPPORTS_FEES
        - SUPPORTS_TAX_TRACKING
        - SUPPORTS_LIABILITY
    ProductRuleKind:
      type: string
      enum:
        - DEPOSIT_CEILING
        - BALANCE_CEILING
        - CONTRIBUTION_CEILING
        - COMBINED_CONTRIBUTION_CEILING
        - ANNUAL_RATE
        - MIN_RATE
        - INTEREST_ACCRUAL_METHOD
        - ELIGIBILITY
        - TAX_REFERENCE
    ProductRule:
      description: >-
        One dated rule of one product, with the publication it was read from.
        Exactly one of `amount`, `percentage` and `text` is set, decided by the
        rule kind.
      type: object
      additionalProperties: false
      required:
        - kind
        - valueType
        - amount
        - percentage
        - text
        - validFrom
        - validTo
        - verification
        - verifiedOn
        - verifiedBy
        - source
      properties:
        kind:
          $ref: '#/components/schemas/ProductRuleKind'
        valueType:
          $ref: '#/components/schemas/RuleValueType'
        amount:
          oneOf:
            - $ref: '#/components/schemas/DecimalAmount'
            - type: 'null'
        percentage:
          description: >-
            A rate expressed in percent as the source published it: `1.7` reads
            1.7 %. It travels as a canonical decimal string, so no client
            multiplies it and no binary float touches a rate.
          oneOf:
            - type: string
              pattern: '^-?(?:0|[1-9][0-9]*)(?:\.[0-9]+)?$'
              examples:
                - '1.7'
            - type: 'null'
        text:
          description: >-
            An uppercase token the interface translates, never a sentence:
            regulatory wording belongs to the source and to the translation
            catalogue.
          oneOf:
            - type: string
              pattern: '^[A-Z][A-Z0-9]*(_[A-Z0-9]+)*$'
              examples:
                - NO_REGULATORY_CONTRIBUTION_CEILING
            - type: 'null'
        validFrom:
          $ref: '#/components/schemas/RuleValidFrom'
        validTo:
          $ref: '#/components/schemas/RuleValidTo'
        verification:
          $ref: '#/components/schemas/RuleVerification'
        verifiedOn:
          oneOf:
            - type: string
              format: date
            - type: 'null'
        verifiedBy:
          oneOf:
            - type: string
            - type: 'null'
        source:
          $ref: '#/components/schemas/ProductRuleSource'
    ProductRuleSource:
      description: The official publication the value was read from.
      type: object
      additionalProperties: false
      required:
        - publisher
        - title
        - url
        - publishedOn
        - retrievedOn
      properties:
        publisher:
          type: string
        title:
          type: string
        url:
          type: string
          format: uri
        publishedOn:
          oneOf:
            - type: string
              format: date
            - type: 'null'
        retrievedOn:
          description: The day a maintainer actually opened the publication.
          type: string
          format: date
    ProductWrapperKind:
      description: >-
        The regulatory or contractual envelope a product sits in. Shared by the
        system catalogue and the product models a workspace describes itself.
      type: string
      enum:
        - NONE
        - REGULATED_SAVINGS
        - TAX_WRAPPER
        - SECURITIES_ACCOUNT
        - LIFE_INSURANCE
        - RETIREMENT
        - EMPLOYEE_SAVINGS
    ProductYieldKind:
      description: >-
        Where a product's return comes from. `MARKET` and `MANUAL_VALUATION`
        carry no rate rule at all: what they earn comes from their assets and
        flows. A model of one of those kinds cannot record a rate period.
      type: string
      enum:
        - NONE
        - REGULATED_RATE
        - CONTRACTUAL_FIXED
        - CONTRACTUAL_VARIABLE
        - MARKET
        - MANUAL_VALUATION
    RuleValueType:
      description: >-
        How a rule's value is written down. A `PERCENTAGE` is expressed in
        percent — `1.7` reads 1.7 % — so no consumer ever multiplies a rate to
        display it.
      type: string
      enum:
        - AMOUNT
        - PERCENTAGE
        - TEXT
    ProductNature:
      description: >-
        Which side of the balance sheet a model sits on, derived from its
        family. Net worth adds one and subtracts the other, so the side is
        stated rather than guessed from a name.
      type: string
      enum:
        - ASSET
        - LIABILITY
    ProductModelOrigin:
      description: >-
        Where a workspace product model came from. A model copied from the
        catalogue can be held against the publication it was copied from; one
        typed by hand answers only for itself. A duplicate stops following its
        source the moment it is created.
      type: string
      enum:
        - DECLARED
        - SYSTEM_PRODUCT
        - WORKSPACE_MODEL
    ProductModelPage:
      type: object
      additionalProperties: false
      required:
        - items
        - page
        - perPage
        - total
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/ProductModel'
        page:
          type: integer
          minimum: 1
        perPage:
          type: integer
          minimum: 1
          maximum: 100
        total:
          type: integer
          minimum: 0
    ProductModel:
      description: >-
        A product model owned by one workspace, beside the read-only system
        catalogue. It carries its own dated rule periods and never a single
        current figure, so last year's statement resolves against last year's
        rate. It is only ever added to: there is no field edit and no period
        deletion.
      type: object
      additionalProperties: false
      required:
        - id
        - name
        - family
        - nature
        - wrapperKind
        - yieldKind
        - yieldGuaranteed
        - ceilingBasis
        - defaultGroupCode
        - valuationMode
        - capabilities
        - origin
        - basedOnProductCode
        - basedOnModelId
        - rules
        - editable
        - version
        - createdAt
        - updatedAt
        - archivedAt
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          minLength: 1
          maxLength: 80
        family:
          $ref: '#/components/schemas/AccountKind'
        nature:
          description: Stated by the server so no interface deduces it from a family name.
          $ref: '#/components/schemas/ProductNature'
        wrapperKind:
          $ref: '#/components/schemas/ProductWrapperKind'
        yieldKind:
          $ref: '#/components/schemas/ProductYieldKind'
        yieldGuaranteed:
          description: >-
            Whether a rate the model carries is owed to the holder. False for a
            revisable or market-driven yield, so no interface shows a rate as an
            acquired return.
          type: boolean
        ceilingBasis:
          $ref: '#/components/schemas/CeilingBasis'
        defaultGroupCode:
          oneOf:
            - type: string
              maxLength: 32
              pattern: '^[A-Z][A-Z0-9]*(_[A-Z0-9]+)*$'
            - type: 'null'
        valuationMode:
          $ref: '#/components/schemas/AccountValuationMode'
        capabilities:
          description: >-
            The public activation contract. Clients never infer behaviour from
            a model name the holder chose.
          type: array
          minItems: 1
          uniqueItems: true
          items:
            $ref: '#/components/schemas/ProductCapability'
        origin:
          $ref: '#/components/schemas/ProductModelOrigin'
        basedOnProductCode:
          description: >-
            The catalogue product this model was started from, kept for reading
            and never for resolving. Null unless `origin` is `SYSTEM_PRODUCT`.
          oneOf:
            - $ref: '#/components/schemas/ProductCode'
            - type: 'null'
        basedOnModelId:
          description: >-
            The model this one was duplicated from. Null unless `origin` is
            `WORKSPACE_MODEL`.
          oneOf:
            - type: string
              format: uuid
            - type: 'null'
        rules:
          description: >-
            Every dated period the model ever recorded, ordered by kind then by
            start date. Periods of the same kind never overlap.
          type: array
          items:
            $ref: '#/components/schemas/ProductModelRule'
        editable:
          description: False for an archived model, which is read-only.
          type: boolean
        version:
          type: integer
          minimum: 1
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        archivedAt:
          oneOf:
            - type: string
              format: date-time
            - type: 'null'
    ProductModelRule:
      description: >-
        One dated period of a model. Exactly one value shape is set, decided by
        the rule kind: an `amount` for a ceiling, a bracket scale for a rate, a
        `text` token for a method or a term. A null `validTo` is in force with
        no known end and is never rendered as an expiry or a zero.
      type: object
      additionalProperties: false
      required:
        - id
        - kind
        - valueType
        - amount
        - text
        - rateApplication
        - brackets
        - validFrom
        - validTo
      properties:
        id:
          type: string
          format: uuid
        kind:
          $ref: '#/components/schemas/ProductRuleKind'
        valueType:
          $ref: '#/components/schemas/RuleValueType'
        amount:
          oneOf:
            - $ref: '#/components/schemas/DecimalAmount'
            - type: 'null'
        text:
          oneOf:
            - type: string
              pattern: '^[A-Z][A-Z0-9]*(_[A-Z0-9]+)*$'
            - type: 'null'
        rateApplication:
          oneOf:
            - $ref: '#/components/schemas/RateApplication'
            - type: 'null'
        brackets:
          description: >-
            The slices a rate scale is made of, in order; empty for a ceiling or
            a term. They tile `[0, +∞[`: the first starts at zero, each starts
            where the previous ended, and the last runs without limit.
          type: array
          items:
            $ref: '#/components/schemas/RateBracket'
        validFrom:
          type: string
          format: date
        validTo:
          oneOf:
            - type: string
              format: date
            - type: 'null'
    ProductModelRuleInput:
      description: >-
        One dated period as a client submits it. The body carries exactly these
        fields; the parser decides which value shape the kind requires and
        refuses the others. Decimals travel as canonical strings — a rate sent
        as a JSON number is refused rather than rounded.
      type: object
      additionalProperties: false
      required:
        - kind
        - amount
        - amountAssetCode
        - text
        - rateApplication
        - brackets
        - validFrom
        - validTo
      properties:
        kind:
          $ref: '#/components/schemas/ProductRuleKind'
        amount:
          description: A ceiling amount as a canonical decimal string, or null for a non-ceiling kind.
          oneOf:
            - type: string
              pattern: '^(?:0|[1-9][0-9]*)(?:\.[0-9]+)?$'
            - type: 'null'
        amountAssetCode:
          oneOf:
            - type: string
              pattern: '^[A-Z][A-Z0-9]{1,11}$'
            - type: 'null'
        text:
          oneOf:
            - type: string
              pattern: '^[A-Z][A-Z0-9]*(_[A-Z0-9]+)*$'
            - type: 'null'
        rateApplication:
          oneOf:
            - $ref: '#/components/schemas/RateApplication'
            - type: 'null'
        brackets:
          description: >-
            One or more brackets for a rate kind, empty otherwise. A single rate
            is the one-bracket case running from zero without limit.
          type: array
          maxItems: 20
          items:
            $ref: '#/components/schemas/RateBracket'
        validFrom:
          type: string
          format: date
        validTo:
          oneOf:
            - type: string
              format: date
            - type: 'null'
    CreateProductModelRequest:
      type: object
      additionalProperties: false
      required:
        - name
        - family
        - wrapperKind
        - yieldKind
        - defaultGroupCode
        - valuationMode
        - capabilities
        - rules
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 80
        family:
          $ref: '#/components/schemas/AccountKind'
        wrapperKind:
          $ref: '#/components/schemas/ProductWrapperKind'
        yieldKind:
          $ref: '#/components/schemas/ProductYieldKind'
        defaultGroupCode:
          oneOf:
            - type: string
              maxLength: 32
              pattern: '^[A-Z][A-Z0-9]*(_[A-Z0-9]+)*$'
            - type: 'null'
        valuationMode:
          $ref: '#/components/schemas/AccountValuationMode'
        capabilities:
          type: array
          minItems: 1
          uniqueItems: true
          items:
            $ref: '#/components/schemas/ProductCapability'
        rules:
          type: array
          maxItems: 20
          items:
            $ref: '#/components/schemas/ProductModelRuleInput'
    CreateProductModelFromProductRequest:
      type: object
      additionalProperties: false
      required:
        - name
        - productCode
        - valuationMode
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 80
        productCode:
          $ref: '#/components/schemas/ProductCode'
        valuationMode:
          $ref: '#/components/schemas/AccountValuationMode'
    DuplicateProductModelRequest:
      type: object
      additionalProperties: false
      required:
        - name
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 80
    AddProductModelRuleRequest:
      description: >-
        A dated period to record, plus the version of the model it is applied
        to. The field set is exact.
      type: object
      additionalProperties: false
      required:
        - kind
        - amount
        - amountAssetCode
        - text
        - rateApplication
        - brackets
        - validFrom
        - validTo
        - version
      properties:
        kind:
          $ref: '#/components/schemas/ProductRuleKind'
        amount:
          oneOf:
            - type: string
              pattern: '^(?:0|[1-9][0-9]*)(?:\.[0-9]+)?$'
            - type: 'null'
        amountAssetCode:
          oneOf:
            - type: string
              pattern: '^[A-Z][A-Z0-9]{1,11}$'
            - type: 'null'
        text:
          oneOf:
            - type: string
              pattern: '^[A-Z][A-Z0-9]*(_[A-Z0-9]+)*$'
            - type: 'null'
        rateApplication:
          oneOf:
            - $ref: '#/components/schemas/RateApplication'
            - type: 'null'
        brackets:
          type: array
          maxItems: 20
          items:
            $ref: '#/components/schemas/RateBracket'
        validFrom:
          type: string
          format: date
        validTo:
          oneOf:
            - type: string
              format: date
            - type: 'null'
        version:
          type: integer
          minimum: 1
    ArchiveProductModelRequest:
      type: object
      additionalProperties: false
      required:
        - version
      properties:
        version:
          type: integer
          minimum: 1
    CategoryPage:
      type: object
      additionalProperties: false
      required:
        - items
        - page
        - perPage
        - total
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Category'
        page:
          type: integer
          minimum: 1
        perPage:
          type: integer
          minimum: 1
          maximum: 100
        total:
          type: integer
          minimum: 0
    Category:
      type: object
      additionalProperties: false
      required:
        - id
        - type
        - label
        - parentId
        - parentLabel
        - icon
        - color
        - defaultAnalyticAxes
        - budgetIncluded
        - sortOrder
        - depth
        - version
        - used
        - typeEditable
        - typeEditReason
        - canAcceptChildren
        - archivedAt
        - replacement
      properties:
        id:
          type: string
          format: uuid
        type:
          $ref: '#/components/schemas/CategoryType'
        label:
          type: string
          minLength: 1
          maxLength: 80
        parentId:
          oneOf:
            - type: string
              format: uuid
            - type: 'null'
        parentLabel:
          description: Label of the parent category, or null when the category is a root.
          oneOf:
            - type: string
              minLength: 1
              maxLength: 80
            - type: 'null'
        icon:
          oneOf:
            - type: string
              pattern: '^[a-z][a-z0-9-]{0,31}$'
            - type: 'null'
        color:
          oneOf:
            - type: string
              pattern: '^#[0-9A-F]{6}$'
            - type: 'null'
        defaultAnalyticAxes:
          $ref: '#/components/schemas/AnalyticAxes'
        budgetIncluded:
          type: boolean
        sortOrder:
          type: integer
          minimum: 0
          maximum: 32767
        depth:
          type: integer
          minimum: 1
          maximum: 8
        version:
          type: integer
          minimum: 1
        used:
          type: boolean
        typeEditable:
          description: Backend-owned eligibility for changing income/expense type.
          type: boolean
        typeEditReason:
          description: Why the type is immutable, or null when it can be changed.
          oneOf:
            - type: string
              enum:
                - ARCHIVED
                - USED
                - CHILD
                - HAS_CHILDREN
            - type: 'null'
        canAcceptChildren:
          description: True only for active categories below the maximum tree depth.
          type: boolean
        archivedAt:
          oneOf:
            - type: string
              format: date-time
            - type: 'null'
        replacement:
          description: Where this category is redirected, or null when it is not.
          oneOf:
            - $ref: '#/components/schemas/CategoryReplacement'
            - type: 'null'
    CategoryLifecycleOperation:
      type: string
      enum:
        - ARCHIVE
        - MERGE
        - MOVE
        - REPLACE
    CategoryImpactBlocker:
      description: Why the impact assessment refuses an operation.
      type: string
      enum:
        - ACTIVE_CHILDREN
        - ALREADY_REDIRECTED
        - CYCLE
        - DEPTH_EXCEEDED
        - REDIRECTION_TARGET
        - REPLACEMENT_CHAIN_TOO_LONG
        - REPLACEMENT_CYCLE
        - SIBLING_LABEL_CONFLICT
        - SOURCE_ARCHIVED
        - TARGET_ARCHIVED
        - TARGET_IS_DESCENDANT
        - TARGET_IS_SELF
        - TARGET_MISSING
        - TARGET_NOT_FOUND
        - TARGET_TYPE_MISMATCH
    CategoryReplacement:
      description: >-
        Where the classifications of a category are redirected. A merge carries
        no date and redirects the whole history; a replacement carries the day
        it takes effect and leaves the period before it untouched.
      type: object
      additionalProperties: false
      required:
        - kind
        - targetId
        - targetLabel
        - effectiveFrom
      properties:
        kind:
          type: string
          enum:
            - MERGE
            - REPLACEMENT
        targetId:
          type: string
          format: uuid
        targetLabel:
          oneOf:
            - type: string
              minLength: 1
              maxLength: 80
            - type: 'null'
        effectiveFrom:
          description: Null for a merge, which redirects the whole history.
          oneOf:
            - type: string
              format: date
            - type: 'null'
    CategoryImpact:
      type: object
      additionalProperties: false
      required:
        - operation
        - categoryId
        - targetId
        - targetLabel
        - effectiveFrom
        - descendantCount
        - archivedDescendantCount
        - reparentedChildCount
        - incomingRedirectionCount
        - resultingDepth
        - maximumDepth
        - archivesSource
        - redirectsHistory
        - allowed
        - blockers
        - affectedClassifications
        - affectedClassificationsReason
      properties:
        operation:
          $ref: '#/components/schemas/CategoryLifecycleOperation'
        categoryId:
          type: string
          format: uuid
        targetId:
          oneOf:
            - type: string
              format: uuid
            - type: 'null'
        targetLabel:
          oneOf:
            - type: string
              minLength: 1
              maxLength: 80
            - type: 'null'
        effectiveFrom:
          oneOf:
            - type: string
              format: date
            - type: 'null'
        descendantCount:
          description: Categories below the source, archived ones included.
          type: integer
          minimum: 0
        archivedDescendantCount:
          type: integer
          minimum: 0
        reparentedChildCount:
          description: Direct children a merge would move under the target.
          type: integer
          minimum: 0
        incomingRedirectionCount:
          description: >-
            Redirections that name this category as their target. A merge carries
            them over to its own target; archiving is refused while any exists,
            because it names no successor to carry them to.
          type: integer
          minimum: 0
        resultingDepth:
          description: >-
            The deepest level the branch would reach afterwards. It may exceed
            `maximumDepth`, which is exactly what `DEPTH_EXCEEDED` reports.
          type: integer
          minimum: 1
        maximumDepth:
          type: integer
          minimum: 1
        archivesSource:
          type: boolean
        redirectsHistory:
          type: boolean
        allowed:
          type: boolean
        blockers:
          type: array
          items:
            $ref: '#/components/schemas/CategoryImpactBlocker'
        affectedClassifications:
          description: >-
            Historical classifications the operation would re-point, or null
            when that count cannot be established. Never an invented `0`.
          oneOf:
            - type: integer
              minimum: 0
            - type: 'null'
        affectedClassificationsReason:
          description: >-
            Why `affectedClassifications` could not be counted, or null when the
            count is known.
          oneOf:
            - type: string
              enum:
                - TRANSACTIONS_UNAVAILABLE
            - type: 'null'
    MoveCategoryRequest:
      type: object
      additionalProperties: false
      required:
        - parentId
        - version
      properties:
        parentId:
          description: The new parent, or null to move the category to the root.
          oneOf:
            - type: string
              format: uuid
            - type: 'null'
        version:
          type: integer
          minimum: 1
    MergeCategoryRequest:
      type: object
      additionalProperties: false
      required:
        - targetId
        - version
      properties:
        targetId:
          type: string
          format: uuid
        version:
          type: integer
          minimum: 1
    ArchiveCategoryRequest:
      type: object
      additionalProperties: false
      required:
        - version
      properties:
        version:
          type: integer
          minimum: 1
    ReplaceCategoryRequest:
      type: object
      additionalProperties: false
      required:
        - targetId
        - effectiveFrom
        - version
      properties:
        targetId:
          type: string
          format: uuid
        effectiveFrom:
          description: The first day the target receives the classifications.
          type: string
          format: date
        version:
          type: integer
          minimum: 1
    CategoryOperationProblem:
      description: >-
        An RFC 9457 problem carrying the refusal reasons as an extension member.
        The members are repeated rather than composed with `allOf`, because
        `Problem` forbids additional properties and a composed schema would
        reject the very document this endpoint returns.
      type: object
      additionalProperties: false
      required:
        - type
        - title
        - status
        - detail
        - blockers
      properties:
        type:
          type: string
          format: uri-reference
          const: /problems/category-operation-refused
        title:
          type: string
        status:
          type: integer
        detail:
          type: string
        blockers:
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/CategoryImpactBlocker'
    CreateCategoryRequest:
      type: object
      additionalProperties: false
      required:
        - type
        - label
        - parentId
        - icon
        - color
        - defaultAnalyticAxes
        - budgetIncluded
        - sortOrder
      properties:
        type:
          $ref: '#/components/schemas/CategoryType'
        label:
          type: string
          minLength: 1
          maxLength: 80
        parentId:
          oneOf:
            - type: string
              format: uuid
            - type: 'null'
        icon:
          oneOf:
            - type: string
              pattern: '^[a-z][a-z0-9-]{0,31}$'
            - type: 'null'
        color:
          oneOf:
            - type: string
              pattern: '^#[0-9A-F]{6}$'
            - type: 'null'
        defaultAnalyticAxes:
          $ref: '#/components/schemas/AnalyticAxes'
        budgetIncluded:
          type: boolean
        sortOrder:
          type: integer
          minimum: 0
          maximum: 32767
    UpdateCategoryRequest:
      type: object
      additionalProperties: false
      required:
        - type
        - label
        - icon
        - color
        - defaultAnalyticAxes
        - budgetIncluded
        - sortOrder
        - version
      properties:
        type:
          $ref: '#/components/schemas/CategoryType'
        label:
          type: string
          minLength: 1
          maxLength: 80
        icon:
          oneOf:
            - type: string
              pattern: '^[a-z][a-z0-9-]{0,31}$'
            - type: 'null'
        color:
          oneOf:
            - type: string
              pattern: '^#[0-9A-F]{6}$'
            - type: 'null'
        defaultAnalyticAxes:
          $ref: '#/components/schemas/AnalyticAxes'
        budgetIncluded:
          type: boolean
        sortOrder:
          type: integer
          minimum: 0
          maximum: 32767
        version:
          type: integer
          minimum: 1
    AccountPage:
      type: object
      additionalProperties: false
      required:
        - items
        - page
        - perPage
        - total
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Account'
        page:
          type: integer
          minimum: 1
        perPage:
          type: integer
          minimum: 1
          maximum: 100
        total:
          type: integer
          minimum: 0
    NetWorthShare:
      description: >-
        Backend-owned weight of one account or exclusive group. Both the ratio
        and the percent are canonical decimal strings. When the share cannot
        be computed it is null with a reason, never `0%`.
      type: object
      additionalProperties: false
      required:
        - ratio
        - percent
        - percentDisplay
        - reason
      properties:
        ratio:
          oneOf:
            - type: string
              pattern: '^-?(?:0|[1-9][0-9]*)(?:\.[0-9]+)?$'
            - type: 'null'
        percent:
          oneOf:
            - type: string
              pattern: '^-?(?:0|[1-9][0-9]*)(?:\.[0-9]+)?$'
            - type: 'null'
        percentDisplay:
          description: >-
            The percent rounded at the presentation boundary, two decimals,
            HALF_UP. A share is almost never a terminating decimal, so the
            exact figure above stays available while this is what a screen
            shows; the interface rounds nothing itself.
          oneOf:
            - type: string
              pattern: '^-?(?:0|[1-9][0-9]*)(?:\.[0-9]+)?$'
            - type: 'null'
        reason:
          oneOf:
            - $ref: '#/components/schemas/NetWorthShareReason'
            - type: 'null'
    NetWorthShareReason:
      type: string
      enum:
        - MISSING_VALUATION
        - MIXED_ASSETS
        - ZERO_ELIGIBLE_NET_WORTH
        - NEGATIVE_ELIGIBLE_NET_WORTH
    AccountGroupPage:
      type: object
      additionalProperties: false
      required:
        - items
        - page
        - perPage
        - total
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/AccountGroup'
        page:
          type: integer
          minimum: 1
        perPage:
          type: integer
          minimum: 1
          maximum: 100
        total:
          type: integer
          minimum: 0
    AccountGroup:
      type: object
      additionalProperties: false
      required:
        - id
        - label
        - parentId
        - parentLabel
        - sortOrder
        - depth
        - version
        - hasChildren
        - canAcceptChildren
        - share
        - archivedAt
      properties:
        id:
          type: string
          format: uuid
        label:
          type: string
          minLength: 1
          maxLength: 80
        parentId:
          oneOf:
            - type: string
              format: uuid
            - type: 'null'
        parentLabel:
          description: Label of the parent group, or null when the group is a root.
          oneOf:
            - type: string
              minLength: 1
              maxLength: 80
            - type: 'null'
        sortOrder:
          type: integer
          minimum: 0
          maximum: 32767
        depth:
          type: integer
          minimum: 1
          maximum: 8
        version:
          type: integer
          minimum: 1
        hasChildren:
          type: boolean
        canAcceptChildren:
          type: boolean
        share:
          $ref: '#/components/schemas/NetWorthShare'
        archivedAt:
          oneOf:
            - type: string
              format: date-time
            - type: 'null'
    CreateAccountGroupRequest:
      type: object
      additionalProperties: false
      required:
        - label
        - parentId
        - sortOrder
      properties:
        label:
          type: string
          minLength: 1
          maxLength: 80
        parentId:
          oneOf:
            - type: string
              format: uuid
            - type: 'null'
        sortOrder:
          type: integer
          minimum: 0
          maximum: 32767
    UpdateAccountGroupRequest:
      type: object
      additionalProperties: false
      required:
        - label
        - parentId
        - sortOrder
        - version
      properties:
        label:
          type: string
          minLength: 1
          maxLength: 80
        parentId:
          oneOf:
            - type: string
              format: uuid
            - type: 'null'
        sortOrder:
          type: integer
          minimum: 0
          maximum: 32767
        version:
          type: integer
          minimum: 1
    ArchiveAccountGroupRequest:
      type: object
      additionalProperties: false
      required:
        - version
      properties:
        version:
          type: integer
          minimum: 1
    Account:
      type: object
      additionalProperties: false
      required:
        - id
        - label
        - assetCode
        - kind
        - productCode
        - productModelId
        - institution
        - maskedIdentifier
        - valuationMode
        - liquidityLevel
        - includeInNetWorth
        - includeInEmergencyFund
        - openedOn
        - closedOn
        - status
        - netWorthSign
        - used
        - editable
        - kindEditable
        - kindEditReason
        - version
        - archivedAt
        - primaryGroupId
        - tagGroupIds
        - share
        - valuation
      properties:
        id:
          type: string
          format: uuid
        label:
          type: string
          minLength: 1
          maxLength: 80
        assetCode:
          description: >-
            The unit the account is denominated in, from the read-only system
            asset reference. It is fixed at creation: changing it would
            reinterpret every figure already recorded against the account.
          type: string
          pattern: '^[A-Z][A-Z0-9]{1,11}$'
        kind:
          $ref: '#/components/schemas/AccountKind'
        productCode:
          description: >-
            The catalogue product this account was created from, or null for an
            account described by hand or from a workspace model. Only the
            reference is stored: ceilings, rates and methods stay in the
            catalogue and are read on the business date they are needed, so a
            regulatory revision is never frozen into an account.
          oneOf:
            - $ref: '#/components/schemas/ProductCode'
            - type: 'null'
        productModelId:
          description: >-
            The reusable workspace product model this account was created
            from, or null for an account described by hand or from a
            catalogue product. An account carries at most one of `productCode`
            and `productModelId`. Only the reference is stored: any dated rule
            the model carries stays on the model and is read on the business
            date it is needed, so archiving the model never changes what an
            existing account resolves.
          oneOf:
            - type: string
              format: uuid
            - type: 'null'
        institution:
          description: >-
            Free text naming where the account is held, such as the bank or the
            insurer. No reference of institutions ships with the application, so
            this is bounded rather than validated against a list.
          oneOf:
            - type: string
              minLength: 1
              maxLength: 80
            - type: 'null'
        maskedIdentifier:
          description: >-
            The visible tail of a banking or contract identifier, such as
            `4821`. Bounded to eight characters, which cannot hold a full
            identifier.
          oneOf:
            - type: string
              pattern: '^[A-Z0-9]{2,8}$'
            - type: 'null'
        valuationMode:
          $ref: '#/components/schemas/AccountValuationMode'
        liquidityLevel:
          $ref: '#/components/schemas/LiquidityLevel'
        includeInNetWorth:
          type: boolean
        includeInEmergencyFund:
          description: Only an account counted in net worth may back the emergency fund.
          type: boolean
        openedOn:
          type: string
          format: date
        closedOn:
          description: The day the account was closed, or null while it is open.
          oneOf:
            - type: string
              format: date
            - type: 'null'
        status:
          $ref: '#/components/schemas/AccountStatus'
        netWorthSign:
          description: >-
            The sign this account contributes to net worth: `1` for an asset,
            `-1` for a liability. It carries no amount; a balance arrives with
            its own record and stays null until then. The sign is independent of
            `includeInNetWorth`: an excluded account still reports its nature, so
            an aggregation must read the inclusion flag before applying the sign.
          type: integer
          enum:
            - -1
            - 1
        used:
          type: boolean
        editable:
          description: False for an archived account, which is read-only.
          type: boolean
        kindEditable:
          description: Backend-owned eligibility for changing the account kind.
          type: boolean
        kindEditReason:
          description: Why the kind is immutable, or null when it can be changed.
          oneOf:
            - type: string
              enum:
                - ARCHIVED
                - USED
            - type: 'null'
        version:
          type: integer
          minimum: 1
        archivedAt:
          oneOf:
            - type: string
              format: date-time
            - type: 'null'
        primaryGroupId:
          description: >-
            Exclusive membership used by group weights. Null until the account
            is assigned. A tag is never this field.
          oneOf:
            - type: string
              format: uuid
            - type: 'null'
        tagGroupIds:
          description: >-
            Extra group labels that never enter an exclusive denominator.
            Each identifier is unique and distinct from `primaryGroupId`.
          type: array
          maxItems: 32
          items:
            type: string
            format: uuid
        share:
          $ref: '#/components/schemas/NetWorthShare'
        valuation:
          $ref: '#/components/schemas/AccountValuation'
    AccountValuation:
      type: object
      additionalProperties: false
      required:
        - accountId
        - requestedOn
        - asOf
        - amount
        - display
        - belowDisplayStep
        - source
        - ageDays
        - quality
        - reconciliationStatus
        - snapshotId
        - version
      properties:
        accountId:
          type: string
          format: uuid
        requestedOn:
          type: string
          format: date
        asOf:
          description: Date of the snapshot that answers, or null when missing.
          oneOf:
            - type: string
              format: date
            - type: 'null'
        amount:
          description: Exact source figure. Null when no snapshot answers.
          oneOf:
            - $ref: '#/components/schemas/DecimalAmount'
            - type: 'null'
        display:
          description: >-
            Source figure rounded at the asset display boundary. Null when
            missing or when a non-zero source would display as zero.
          oneOf:
            - $ref: '#/components/schemas/DecimalAmount'
            - type: 'null'
        belowDisplayStep:
          type: boolean
        source:
          oneOf:
            - $ref: '#/components/schemas/BalanceSnapshotSource'
            - type: 'null'
        ageDays:
          oneOf:
            - type: integer
              minimum: 0
            - type: 'null'
        quality:
          $ref: '#/components/schemas/ValuationQuality'
        reconciliationStatus:
          oneOf:
            - $ref: '#/components/schemas/ReconciliationStatus'
            - type: 'null'
        snapshotId:
          oneOf:
            - type: string
              format: uuid
            - type: 'null'
        version:
          oneOf:
            - type: integer
              minimum: 1
            - type: 'null'
    ValuationQuality:
      type: string
      enum:
        - CURRENT
        - STALE
        - MISSING
    BalanceSnapshotSource:
      type: string
      enum:
        - MANUAL
        - IMPORT
        - BANK_API
        - CALCULATED
    ReconciliationStatus:
      type: string
      enum:
        - UNRECONCILED
        - RECONCILED
    AccountBalanceSnapshot:
      type: object
      additionalProperties: false
      required:
        - id
        - accountId
        - asOf
        - amount
        - source
        - reconciliationStatus
        - comment
        - active
        - version
        - recordedAt
        - supersededAt
      properties:
        id:
          type: string
          format: uuid
        accountId:
          type: string
          format: uuid
        asOf:
          type: string
          format: date
        amount:
          $ref: '#/components/schemas/DecimalAmount'
        source:
          $ref: '#/components/schemas/BalanceSnapshotSource'
        reconciliationStatus:
          $ref: '#/components/schemas/ReconciliationStatus'
        comment:
          oneOf:
            - type: string
              minLength: 1
              maxLength: 200
            - type: 'null'
        active:
          type: boolean
        version:
          type: integer
          minimum: 1
        recordedAt:
          type: string
          format: date-time
        supersededAt:
          oneOf:
            - type: string
              format: date-time
            - type: 'null'
    AccountBalanceSnapshotPage:
      type: object
      additionalProperties: false
      required:
        - items
        - page
        - perPage
        - total
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/AccountBalanceSnapshot'
        page:
          type: integer
          minimum: 1
        perPage:
          type: integer
          minimum: 1
          maximum: 100
        total:
          type: integer
          minimum: 0
    RecordAccountBalanceRequest:
      type: object
      additionalProperties: false
      required:
        - asOf
        - amount
        - amountAssetCode
        - comment
        - version
      properties:
        asOf:
          type: string
          format: date
        amount:
          type: string
          pattern: '^(?!-0(?:\.0+)?$)-?(?:0|[1-9][0-9]*)(?:\.[0-9]+)?$'
        amountAssetCode:
          type: string
          pattern: '^[A-Z][A-Z0-9]{1,11}$'
        comment:
          oneOf:
            - type: string
              minLength: 1
              maxLength: 200
            - type: 'null'
        version:
          description: Required when replacing the active snapshot of that day and source.
          oneOf:
            - type: integer
              minimum: 1
            - type: 'null'
    CreateAccountRequest:
      type: object
      additionalProperties: false
      required:
        - label
        - assetCode
        - kind
        - productCode
        - productModelId
        - institution
        - maskedIdentifier
        - valuationMode
        - liquidityLevel
        - includeInNetWorth
        - includeInEmergencyFund
        - openedOn
        - closedOn
        - primaryGroupId
        - tagGroupIds
      properties:
        label:
          type: string
          minLength: 1
          maxLength: 80
        assetCode:
          type: string
          pattern: '^[A-Z][A-Z0-9]{1,11}$'
        kind:
          description: >-
            Must equal the `accountKind` or `family` the referenced product or
            model declares. That reference is the authority, so a tampered
            form cannot file a PEA as a plain savings account.
          $ref: '#/components/schemas/AccountKind'
        productCode:
          description: >-
            A catalogue product to create the account from, or null for an
            account described by hand or from a workspace model. Mutually
            exclusive with `productModelId`. An unknown product is refused by
            the use case and by a foreign key.
          oneOf:
            - $ref: '#/components/schemas/ProductCode'
            - type: 'null'
        productModelId:
          description: >-
            A reusable workspace product model to create the account from, or
            null for an account described by hand or from a catalogue product.
            Mutually exclusive with `productCode`. The model must belong to
            the calling workspace; an unknown or foreign identifier is
            refused by the use case and by a foreign key. An archived model
            is refused by the use case only: the foreign key still accepts
            it, so an account already created from that model keeps resolving.
          oneOf:
            - type: string
              format: uuid
            - type: 'null'
        institution:
          oneOf:
            - type: string
              minLength: 1
              maxLength: 80
            - type: 'null'
        maskedIdentifier:
          oneOf:
            - type: string
              pattern: '^[A-Z0-9]{2,8}$'
            - type: 'null'
        valuationMode:
          $ref: '#/components/schemas/AccountValuationMode'
        liquidityLevel:
          $ref: '#/components/schemas/LiquidityLevel'
        includeInNetWorth:
          type: boolean
        includeInEmergencyFund:
          type: boolean
        openedOn:
          type: string
          format: date
        closedOn:
          oneOf:
            - type: string
              format: date
            - type: 'null'
        primaryGroupId:
          description: >-
            Optional exclusive membership used by group weights. Null leaves
            the account ungrouped. A tag is never this field.
          oneOf:
            - type: string
              format: uuid
            - type: 'null'
        tagGroupIds:
          description: >-
            Extra group labels that never enter an exclusive denominator.
            Each identifier is unique and distinct from `primaryGroupId`.
          type: array
          maxItems: 32
          items:
            type: string
            format: uuid
    UpdateAccountRequest:
      type: object
      additionalProperties: false
      required:
        - label
        - kind
        - productCode
        - productModelId
        - institution
        - maskedIdentifier
        - valuationMode
        - liquidityLevel
        - includeInNetWorth
        - includeInEmergencyFund
        - openedOn
        - closedOn
        - version
        - primaryGroupId
        - tagGroupIds
      properties:
        label:
          type: string
          minLength: 1
          maxLength: 80
        kind:
          $ref: '#/components/schemas/AccountKind'
        productCode:
          description: >-
            Changing the product, the kind or the valuation mode sends the whole
            triple back through the catalogue. An account already used by
            history cannot change product at all. Mutually exclusive with
            `productModelId`.
          oneOf:
            - $ref: '#/components/schemas/ProductCode'
            - type: 'null'
        productModelId:
          description: >-
            Changing the model, the kind or the valuation mode sends the whole
            triple back through the workspace's own models. An account
            already used by history cannot change model at all. Mutually
            exclusive with `productCode`.
          oneOf:
            - type: string
              format: uuid
            - type: 'null'
        institution:
          oneOf:
            - type: string
              minLength: 1
              maxLength: 80
            - type: 'null'
        maskedIdentifier:
          oneOf:
            - type: string
              pattern: '^[A-Z0-9]{2,8}$'
            - type: 'null'
        valuationMode:
          $ref: '#/components/schemas/AccountValuationMode'
        liquidityLevel:
          $ref: '#/components/schemas/LiquidityLevel'
        includeInNetWorth:
          type: boolean
        includeInEmergencyFund:
          type: boolean
        openedOn:
          type: string
          format: date
        closedOn:
          description: A day on or after the opening date closes the account; null reopens it.
          oneOf:
            - type: string
              format: date
            - type: 'null'
        version:
          type: integer
          minimum: 1
        primaryGroupId:
          description: >-
            Exclusive membership used by group weights, or null to leave the
            account ungrouped. An unknown or foreign group is refused.
          oneOf:
            - type: string
              format: uuid
            - type: 'null'
        tagGroupIds:
          description: Extra group labels that never enter an exclusive denominator.
          type: array
          maxItems: 32
          items:
            type: string
            format: uuid
    ArchiveAccountRequest:
      type: object
      additionalProperties: false
      required:
        - version
      properties:
        version:
          type: integer
          minimum: 1
    AccountKind:
      description: >-
        What an account holds. Shared by the product catalogue and the accounts
        of a workspace, so a product-based account inherits exactly one of these
        kinds.
      type: string
      enum:
        - CURRENT
        - SAVINGS
        - PORTFOLIO
        - INSURANCE_CONTRACT
        - EMPLOYEE_BENEFIT
        - CASH
        - REAL_ASSET
        - LIABILITY
    AccountStatus:
      type: string
      enum:
        - ACTIVE
        - CLOSED
        - ARCHIVED
    AccountValuationMode:
      description: >-
        Where the value of the account comes from: recorded movements, dated
        valuations entered by hand, or portfolio positions. Only an account
        kind that holds positions accepts `PORTFOLIO`.
      type: string
      enum:
        - TRANSACTIONS
        - SNAPSHOTS
        - PORTFOLIO
    LiquidityLevel:
      type: string
      enum:
        - IMMEDIATE
        - SHORT_TERM
        - MEDIUM_TERM
        - LONG_TERM
        - ILLIQUID
    AccountRules:
      description: >-
        Everything in force for one account on one business date, split into
        the three questions that are answered differently: what caps it, what
        it earns, and under which terms.

        Each entry shows the same rule through every authority that states it,
        so a published figure and one the holder typed are never confused for
        one another.
      type: object
      additionalProperties: false
      required:
        - accountId
        - assetCode
        - productCode
        - productModelId
        - origin
        - asOf
        - ceilings
        - rates
        - terms
        - unavailableRuleKinds
        - yieldReading
      properties:
        accountId:
          type: string
          format: uuid
        assetCode:
          description: The unit the account is denominated in.
          type: string
          pattern: '^[A-Z][A-Z0-9]{1,11}$'
        productCode:
          description: >-
            The system product behind these rules: the one the account
            references, or the one the workspace model it follows was copied
            from. Null when no catalogue product is in the chain at all.
          oneOf:
            - $ref: '#/components/schemas/ProductCode'
            - type: 'null'
        productModelId:
          description: >-
            The workspace product model these rules were read from, or null
            unless `origin` is `WORKSPACE_MODEL`.
          oneOf:
            - type: string
              format: uuid
            - type: 'null'
        origin:
          $ref: '#/components/schemas/AccountRulesOrigin'
        asOf:
          description: >-
            The business date the rules were resolved on. Every value below
            answers for this date and for no other.
          type: string
          format: date
        ceilings:
          type: array
          items:
            $ref: '#/components/schemas/AccountCeilingRule'
        rates:
          type: array
          items:
            $ref: '#/components/schemas/AccountRateRule'
        terms:
          type: array
          items:
            $ref: '#/components/schemas/AccountTermRule'
        unavailableRuleKinds:
          description: >-
            The rules this account is expected to carry and that no layer
            covers on this date. A gap is shown as unavailable and never as
            zero, which would read as "no ceiling" or "no interest" when the
            truth is that the value is unknown. A kind a local override answers
            for is not a gap. A catalogue figure for a kind the workspace
            model never carried is also a gap: the model stopped following
            the publication, and the catalogue layer is comparison only for
            kinds the model already states.
          type: array
          items:
            $ref: '#/components/schemas/ProductRuleKind'
        yieldReading:
          $ref: '#/components/schemas/AccountYieldReading'
    AccountYieldReading:
      description: >-
        The three readings a rate screen must keep apart: the contractual or
        regulated rate, an optional projection assumption, and observed
        performance. An assumption is never stored as a rate on a market
        product. Observed performance is not computed from a pair of
        valuations: inventing a return from two stocks would present a
        movement as a yield.
      type: object
      additionalProperties: false
      required:
        - contractual
        - assumption
        - assumptionReason
        - observed
        - observedReason
      properties:
        contractual:
          oneOf:
            - type: object
              additionalProperties: false
              required:
                - kind
                - guaranteed
              properties:
                kind:
                  $ref: '#/components/schemas/ProductRuleKind'
                guaranteed:
                  type: boolean
            - type: 'null'
        assumption:
          description: A projection rate is not recorded as a product rule.
          type: 'null'
        assumptionReason:
          type: string
          enum:
            - NOT_RECORDED
        observed:
          description: No return series is computed from account valuations.
          type: 'null'
        observedReason:
          type: string
          enum:
            - NO_RETURN_SERIES
    AccountRulesOrigin:
      description: >-
        Where the account reads the rules it inherits. An account that inherits
        no rule and an account whose rules could not be read are different
        answers, and neither of them means the account has no ceiling.
        `WORKSPACE_MODEL` rules carry no `verification` or `source`: nobody
        published them. Overrides are independent of this: an account keeps
        answering with the claims recorded against it whatever its origin.
      type: string
      enum:
        - NO_PRODUCT
        - SYSTEM_CATALOG
        - PRODUCT_WITHDRAWN
        - WORKSPACE_MODEL
    AccountRuleLayer:
      description: >-
        Which authority a resolved value came from. `CATALOG` is the system
        publication, `INHERITED` the workspace product model the account
        follows, `OVERRIDE` a value recorded against this account alone. A
        local claim wins over the model, which wins over the catalogue.
      type: string
      enum:
        - CATALOG
        - INHERITED
        - OVERRIDE
    AccountCeilingRule:
      description: >-
        One ceiling kind seen through every authority that states it, and which
        of them wins. At least one of `catalog`, `inherited` and `override` is
        present.
      type: object
      additionalProperties: false
      required:
        - kind
        - basis
        - countsCreditedInterest
        - spansSeveralAccounts
        - effectiveLayer
        - catalog
        - inherited
        - override
        - check
      properties:
        kind:
          $ref: '#/components/schemas/ProductRuleKind'
        basis:
          $ref: '#/components/schemas/CeilingBasis'
        countsCreditedInterest:
          description: >-
            Whether interest credited by the institution counts towards the
            measure. When it does not, interest may carry the account past the
            amount without any rule being broken. It follows from the rule
            kind, so it is the same on every layer.
          type: boolean
        spansSeveralAccounts:
          description: >-
            Whether the ceiling is reached across the products sharing it, in
            which case reading this account alone cannot settle it.
          type: boolean
        effectiveLayer:
          $ref: '#/components/schemas/AccountRuleLayer'
        catalog:
          oneOf:
            - $ref: '#/components/schemas/AccountCeiling'
            - type: 'null'
        inherited:
          oneOf:
            - $ref: '#/components/schemas/AccountCeiling'
            - type: 'null'
        override:
          oneOf:
            - $ref: '#/components/schemas/AccountCeiling'
            - type: 'null'
        check:
          $ref: '#/components/schemas/CeilingCheck'
    CeilingCheck:
      description: >-
        Comparison of a measured figure to the effective ceiling. EXCEEDED is
        a warning, never a refusal: a Livret Bleu, credited interest or an
        imported history may sit above the published amount. The scale, not
        this status, decides what the excess earns.
      type: object
      additionalProperties: false
      required:
        - status
        - warning
        - measured
        - excess
        - unsettledReason
      properties:
        status:
          type: string
          enum:
            - WITHIN
            - EXCEEDED
            - NOT_COMPARABLE
            - UNSETTLED
        warning:
          type: boolean
        measured:
          oneOf:
            - $ref: '#/components/schemas/DecimalAmount'
            - type: 'null'
        excess:
          oneOf:
            - $ref: '#/components/schemas/DecimalAmount'
            - type: 'null'
        unsettledReason:
          oneOf:
            - type: string
              enum:
                - MISSING_VALUATION
                - CONTRIBUTIONS_NOT_TRACKED
                - COMBINED_CEILING
            - type: 'null'
    AccountCeiling:
      description: One authority's ceiling for the account, with its provenance.
      type: object
      additionalProperties: false
      required:
        - measurable
        - amount
        - validFrom
        - validTo
        - verification
        - source
        - claim
      properties:
        measurable:
          description: >-
            False when the ceiling is published in another unit than the
            account is denominated in. No conversion is applied and no verdict
            is produced; the ceiling is reported as not comparable. An override
            is always measurable: a local claim is recorded in the account's
            own unit.
          type: boolean
        amount:
          $ref: '#/components/schemas/DecimalAmount'
        validFrom:
          $ref: '#/components/schemas/RuleValidFrom'
        validTo:
          $ref: '#/components/schemas/RuleValidTo'
        verification:
          $ref: '#/components/schemas/DeclaredOrVerified'
        source:
          $ref: '#/components/schemas/DeclaredOrSourced'
        claim:
          $ref: '#/components/schemas/AccountRuleClaimOrNull'
    AccountRateRule:
      description: >-
        One rate kind seen through every authority that states it, and which of
        them wins. At least one of `catalog`, `inherited` and `override` is
        present.
      type: object
      additionalProperties: false
      required:
        - kind
        - effectiveLayer
        - catalog
        - inherited
        - override
        - applied
      properties:
        kind:
          $ref: '#/components/schemas/ProductRuleKind'
        effectiveLayer:
          $ref: '#/components/schemas/AccountRuleLayer'
        catalog:
          oneOf:
            - $ref: '#/components/schemas/AccountRate'
            - type: 'null'
        inherited:
          oneOf:
            - $ref: '#/components/schemas/AccountRate'
            - type: 'null'
        override:
          oneOf:
            - $ref: '#/components/schemas/AccountRate'
            - type: 'null'
        applied:
          description: >-
            How the effective scale reads the latest recorded balance. Null
            when no valuation exists on the business date. A Livret Bleu
            above the Livret A figure shifts onto the next bracket; that is
            allowed, not a refusal.
          oneOf:
            - $ref: '#/components/schemas/AppliedRate'
            - type: 'null'
    AppliedRate:
      type: object
      additionalProperties: false
      required:
        - interest
        - effectivePercentage
        - reachedPercentage
        - reachedLowerBound
        - rateShiftsAboveFirstBracket
        - unsettledReason
      properties:
        interest:
          oneOf:
            - type: string
            - type: 'null'
        effectivePercentage:
          oneOf:
            - type: string
            - type: 'null'
        reachedPercentage:
          type: string
        reachedLowerBound:
          type: string
        rateShiftsAboveFirstBracket:
          type: boolean
        unsettledReason:
          oneOf:
            - type: string
              enum:
                - ZERO_BALANCE
                - NEGATIVE_BALANCE
            - type: 'null'
    AccountRate:
      description: >-
        One authority's rate for the account, always as a bracket scale. The
        products the catalogue ships today resolve to a single bracket running
        from zero without limit, so a tiered product arrives later as data
        rather than as a second way of reading a rate.
      type: object
      additionalProperties: false
      required:
        - guaranteed
        - application
        - brackets
        - validFrom
        - validTo
        - verification
        - source
        - claim
      properties:
        guaranteed:
          description: >-
            Whether the rate is owed to the holder. A revisable rate is only
            the rate published today and is never an acquired return. A local
            claim is graded by the promise of the product behind it, never by
            its own say-so.
          type: boolean
        application:
          $ref: '#/components/schemas/RateApplication'
        brackets:
          description: >-
            The slices the scale is made of, in order. They tile the amounts
            they cover: the first starts at zero, each one starts where the
            previous ended, and the last runs without limit.
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/RateBracket'
        validFrom:
          $ref: '#/components/schemas/RuleValidFrom'
        validTo:
          $ref: '#/components/schemas/RuleValidTo'
        verification:
          $ref: '#/components/schemas/DeclaredOrVerified'
        source:
          $ref: '#/components/schemas/DeclaredOrSourced'
        claim:
          $ref: '#/components/schemas/AccountRuleClaimOrNull'
    RateApplication:
      description: >-
        How the brackets combine over an amount. `MARGINAL` remunerates each
        slice at the rate of the bracket covering it, while `FLAT_BY_BRACKET`
        applies the rate of the bracket reached to the whole amount. A scale of
        4 % up to 10 000 then 2 % above pays 500 on 15 000 under the first and
        300 under the second, so a scale is never read without its mode. A
        single-bracket scale gives the same result under both.
      type: string
      enum:
        - MARGINAL
        - FLAT_BY_BRACKET
    RateBracket:
      description: >-
        One slice of a rate scale. The lower bound is included and the upper
        bound excluded, so two adjacent brackets leave no amount uncovered and
        count none twice.
      type: object
      additionalProperties: false
      required:
        - percentage
        - lowerBound
        - upperBound
      properties:
        percentage:
          description: >-
            The rate expressed in percent as the source published it: `1.7`
            reads 1.7 %. It travels as a canonical decimal string, so no client
            multiplies it and no binary float touches a rate.
          type: string
          pattern: '^-?(?:0|[1-9][0-9]*)(?:\.[0-9]+)?$'
          examples:
            - '1.7'
        lowerBound:
          description: First amount the bracket covers, included.
          type: string
          pattern: '^(?:0|[1-9][0-9]*)(?:\.[0-9]+)?$'
          examples:
            - '0'
        upperBound:
          description: >-
            First amount the bracket no longer covers, or null when the slice
            runs without limit.
          oneOf:
            - type: string
              pattern: '^(?:0|[1-9][0-9]*)(?:\.[0-9]+)?$'
            - type: 'null'
    AccountTermRule:
      description: >-
        One term kind — how interest accrues, who is eligible, which tax regime
        applies — seen through every authority that states it. At least one of
        `catalog`, `inherited` and `override` is present.
      type: object
      additionalProperties: false
      required:
        - kind
        - effectiveLayer
        - catalog
        - inherited
        - override
      properties:
        kind:
          $ref: '#/components/schemas/ProductRuleKind'
        effectiveLayer:
          $ref: '#/components/schemas/AccountRuleLayer'
        catalog:
          oneOf:
            - $ref: '#/components/schemas/AccountTerm'
            - type: 'null'
        inherited:
          oneOf:
            - $ref: '#/components/schemas/AccountTerm'
            - type: 'null'
        override:
          oneOf:
            - $ref: '#/components/schemas/AccountTerm'
            - type: 'null'
    AccountTerm:
      description: >-
        One authority's rule for the account that states neither a ceiling nor
        a rate.
      type: object
      additionalProperties: false
      required:
        - token
        - validFrom
        - validTo
        - verification
        - source
        - claim
      properties:
        token:
          description: >-
            An uppercase token the interface translates, never a sentence:
            regulatory wording belongs to the publication it was read from and
            to the translation catalogue.
          type: string
          pattern: '^[A-Z][A-Z0-9]*(_[A-Z0-9]+)*$'
          examples:
            - NO_REGULATORY_CONTRIBUTION_CEILING
        validFrom:
          $ref: '#/components/schemas/RuleValidFrom'
        validTo:
          $ref: '#/components/schemas/RuleValidTo'
        verification:
          $ref: '#/components/schemas/DeclaredOrVerified'
        source:
          $ref: '#/components/schemas/DeclaredOrSourced'
        claim:
          $ref: '#/components/schemas/AccountRuleClaimOrNull'
    DeclaredOrVerified:
      description: >-
        Null for a value nobody published — one read from a workspace product
        model, and one claimed by an account override. Grading the freshness of
        a figure the holder typed would fabricate a provenance it never had.
      oneOf:
        - $ref: '#/components/schemas/RuleVerification'
        - type: 'null'
    DeclaredOrSourced:
      description: >-
        Null for a value nobody published — one read from a workspace product
        model, and one claimed by an account override. There is no publication
        to name.
      oneOf:
        - $ref: '#/components/schemas/ProductRuleSource'
        - type: 'null'
    AccountRuleClaimOrNull:
      description: >-
        Who claimed this value and why, or null when nobody claimed it and it
        was inherited or published.
      oneOf:
        - $ref: '#/components/schemas/AccountRuleClaim'
        - type: 'null'
    AccountRuleClaim:
      description: >-
        The local claim behind an override, without repeating the value the
        rule already carries.
      type: object
      additionalProperties: false
      required:
        - overrideId
        - reason
        - authorId
        - authorDisplayName
        - recordedAt
      properties:
        overrideId:
          type: string
          format: uuid
        reason:
          description: >-
            Why this account diverges from what it inherits, in the holder's
            own words. It is required: an unexplained local figure sitting
            beside a published one is exactly the drift an override has to make
            visible instead of hiding.
          type: string
          minLength: 1
          maxLength: 200
        authorId:
          description: Who recorded the claim. It comes from the session, never from a request body.
          type: string
          format: uuid
        authorDisplayName:
          description: The author's display name, or null when the account no longer exists.
          oneOf:
            - type: string
            - type: 'null'
        recordedAt:
          type: string
          format: date-time
    AccountRuleOverridePage:
      type: object
      additionalProperties: false
      required:
        - overrides
      properties:
        overrides:
          description: >-
            Every override ever recorded on the account, withdrawn ones
            included, ordered by rule kind then by the day each period starts.
          type: array
          items:
            $ref: '#/components/schemas/AccountRuleOverride'
    AccountRuleOverride:
      description: >-
        One dated rule value recorded against a single account, in front of
        whatever that account inherits.
      type: object
      additionalProperties: false
      required:
        - id
        - accountId
        - kind
        - valueType
        - amount
        - text
        - application
        - brackets
        - validFrom
        - validTo
        - standing
        - withdrawnAt
        - withdrawnBy
        - reason
        - authorId
        - authorDisplayName
        - recordedAt
      properties:
        id:
          type: string
          format: uuid
        accountId:
          type: string
          format: uuid
        kind:
          $ref: '#/components/schemas/ProductRuleKind'
        valueType:
          description: >-
            Which of `amount`, `text` and `brackets` carries the value. It
            follows from the rule kind, so a client reads the one its kind
            declares instead of guessing.
          $ref: '#/components/schemas/RuleValueType'
        amount:
          oneOf:
            - $ref: '#/components/schemas/DecimalAmount'
            - type: 'null'
        text:
          oneOf:
            - type: string
              pattern: '^[A-Z][A-Z0-9]*(_[A-Z0-9]+)*$'
            - type: 'null'
        application:
          oneOf:
            - $ref: '#/components/schemas/RateApplication'
            - type: 'null'
        brackets:
          oneOf:
            - type: array
              minItems: 1
              items:
                $ref: '#/components/schemas/RateBracket'
            - type: 'null'
        validFrom:
          description: First day the claim applies.
          type: string
          format: date
        validTo:
          $ref: '#/components/schemas/RuleValidTo'
        standing:
          description: >-
            False once the override has been withdrawn. A withdrawn claim stops
            answering on every date, past ones included, and the inherited rule
            of each of those dates takes over again. It is kept here because
            the trail of what was claimed is the provenance an override exists
            to preserve.
          type: boolean
        withdrawnAt:
          oneOf:
            - type: string
              format: date-time
            - type: 'null'
        withdrawnBy:
          oneOf:
            - type: string
              format: uuid
            - type: 'null'
        reason:
          type: string
          minLength: 1
          maxLength: 200
        authorId:
          type: string
          format: uuid
        authorDisplayName:
          description: The author's display name, or null when the account no longer exists.
          oneOf:
            - type: string
            - type: 'null'
        recordedAt:
          type: string
          format: date-time
    RecordAccountRuleOverrideRequest:
      type: object
      additionalProperties: false
      required:
        - kind
        - amount
        - amountAssetCode
        - text
        - rateApplication
        - brackets
        - validFrom
        - validTo
        - reason
      properties:
        kind:
          $ref: '#/components/schemas/ProductRuleKind'
        amount:
          description: >-
            The ceiling amount as a canonical decimal string, for a kind whose
            value is an amount, and null otherwise. It never travels as a JSON
            number, which a binary float would already have rounded.
          oneOf:
            - type: string
              pattern: '^(?:0|[1-9][0-9]*)(?:\.[0-9]+)?$'
            - type: 'null'
        amountAssetCode:
          description: >-
            The unit the amount is denominated in. It must be the unit the
            account itself is held in: a local ceiling is checked against this
            account and nothing else, and no conversion ships with the
            application.
          oneOf:
            - type: string
              pattern: '^[A-Z][A-Z0-9]{1,11}$'
            - type: 'null'
        text:
          oneOf:
            - type: string
              pattern: '^[A-Z][A-Z0-9]*(_[A-Z0-9]+)*$'
              maxLength: 64
            - type: 'null'
        rateApplication:
          oneOf:
            - $ref: '#/components/schemas/RateApplication'
            - type: 'null'
        brackets:
          description: >-
            The slices of a rate scale, for a rate kind, and an empty list
            otherwise. A single rate is the one-bracket case of the same shape.
          type: array
          maxItems: 20
          items:
            $ref: '#/components/schemas/RateBracket'
        validFrom:
          description: First day the claim applies.
          type: string
          format: date
        validTo:
          $ref: '#/components/schemas/RuleValidTo'
        reason:
          description: Why this account diverges from what it inherits.
          type: string
          minLength: 1
          maxLength: 200
    EmptyRequest:
      description: >-
        A body carrying no field. It is still required and still narrowed, so a
        replayed form cannot smuggle one in and a client cannot believe a
        payload it sent was applied.
      type: object
      additionalProperties: false
    RuleValidFrom:
      description: First day this catalogue vouches for the value.
      type: string
      format: date
    RuleValidTo:
      description: >-
        Last day covered, inclusive, or null while no revision has closed the
        period. An open end is in force for every later business date; it is
        never an expiry.
      oneOf:
        - type: string
          format: date
        - type: 'null'
    RuleVerification:
      description: >-
        How much the value can be trusted today, independently of the business
        date it applies to. `STALE` means nobody has re-read the source across
        a full revision cycle.
      type: string
      enum:
        - VERIFIED
        - STALE
        - UNVERIFIED
    CategoryType:
      type: string
      enum:
        - EXPENSE
        - INCOME
    AnalyticAxes:
      type: array
      uniqueItems: true
      maxItems: 6
      items:
        type: string
        enum:
          - DISCRETIONARY
          - ESSENTIAL
          - FIXED
          - PERSONAL
          - PROFESSIONAL
          - VARIABLE
    NetWorthReason:
      description: >-
        Why a net-worth figure, a delta or a change rate could not be produced.
        A reason replaces the figure and never accompanies one.
      type: string
      enum:
        - MISSING_VALUATION
        - NO_ELIGIBLE_ACCOUNT
        - MIXED_ASSETS
        - ZERO_BASE
        - NEGATIVE_BASE
    NetWorthAmount:
      description: >-
        One published figure: the exact source decimal with its asset, and the
        same figure rounded at the asset display boundary. A non-zero source
        that would display as zero is reported as below the step instead of
        becoming an invented nil.
      type: object
      additionalProperties: false
      required:
        - value
        - assetCode
        - display
        - belowDisplayStep
      properties:
        value:
          type: string
          pattern: '^-?(?:0|[1-9][0-9]*)(?:\.[0-9]+)?$'
        assetCode:
          type: string
          pattern: '^[A-Z][A-Z0-9]{1,11}$'
        display:
          oneOf:
            - $ref: '#/components/schemas/DecimalAmount'
            - type: 'null'
        belowDisplayStep:
          type: boolean
    NetWorthDelta:
      description: >-
        How net worth moved between two business days. The amount and the rate
        carry their own reason, because they fail for different causes.
      type: object
      additionalProperties: false
      required:
        - comparedOn
        - previousTotal
        - amount
        - amountReason
        - rate
        - ratePercent
        - ratePercentDisplay
        - rateReason
      properties:
        comparedOn:
          type: string
          format: date
        previousTotal:
          oneOf:
            - $ref: '#/components/schemas/NetWorthAmount'
            - type: 'null'
        amount:
          oneOf:
            - $ref: '#/components/schemas/NetWorthAmount'
            - type: 'null'
        amountReason:
          oneOf:
            - $ref: '#/components/schemas/NetWorthReason'
            - type: 'null'
        rate:
          description: Signed ratio as a canonical decimal string, never a percentage.
          oneOf:
            - type: string
              pattern: '^-?(?:0|[1-9][0-9]*)(?:\.[0-9]+)?$'
            - type: 'null'
        ratePercent:
          description: >-
            The same ratio expressed in percent by the backend, so no interface
            multiplies a decimal itself.
          oneOf:
            - type: string
              pattern: '^-?(?:0|[1-9][0-9]*)(?:\.[0-9]+)?$'
            - type: 'null'
        ratePercentDisplay:
          description: >-
            The percent rounded at the presentation boundary, two decimals,
            HALF_UP. A change rate is almost never a terminating decimal, so
            the exact figure above stays available while this is what a screen
            shows; the interface rounds nothing itself.
          oneOf:
            - type: string
              pattern: '^-?(?:0|[1-9][0-9]*)(?:\.[0-9]+)?$'
            - type: 'null'
        rateReason:
          oneOf:
            - $ref: '#/components/schemas/NetWorthReason'
            - type: 'null'
    NetWorthContribution:
      description: >-
        One contributing account. `amount` is the stored figure as it appears
        on a statement; `signedAmount` is what it adds to net worth.
      type: object
      additionalProperties: false
      required:
        - accountId
        - label
        - kind
        - netWorthSign
        - primaryGroupId
        - primaryGroupLabel
        - eligible
        - amount
        - signedAmount
        - quality
        - ageDays
        - valuedOn
        - share
      properties:
        accountId:
          type: string
          format: uuid
        label:
          type: string
        kind:
          $ref: '#/components/schemas/AccountKind'
        netWorthSign:
          type: integer
          enum:
            - -1
            - 1
        primaryGroupId:
          oneOf:
            - type: string
              format: uuid
            - type: 'null'
        primaryGroupLabel:
          oneOf:
            - type: string
            - type: 'null'
        eligible:
          description: Open on the requested day and included in net worth.
          type: boolean
        amount:
          oneOf:
            - $ref: '#/components/schemas/NetWorthAmount'
            - type: 'null'
        signedAmount:
          oneOf:
            - $ref: '#/components/schemas/NetWorthAmount'
            - type: 'null'
        quality:
          $ref: '#/components/schemas/ValuationQuality'
        ageDays:
          oneOf:
            - type: integer
              minimum: 0
            - type: 'null'
        valuedOn:
          oneOf:
            - type: string
              format: date
            - type: 'null'
        share:
          $ref: '#/components/schemas/NetWorthShare'
    NetWorthAllocationEntry:
      description: >-
        The exclusive weight of one group: its own accounts and those of its
        descendants, counted once. A tag never enters this figure, so two
        groups of the same depth never claim the same account. They do not add
        up to the published total: an account with no primary group belongs to
        no bucket, and a group weighed against a small eligible base can exceed
        100 %.
      type: object
      additionalProperties: false
      required:
        - groupId
        - label
        - parentId
        - depth
        - value
        - share
      properties:
        groupId:
          type: string
          format: uuid
        label:
          type: string
        parentId:
          oneOf:
            - type: string
              format: uuid
            - type: 'null'
        depth:
          type: integer
          minimum: 1
          maximum: 8
        value:
          oneOf:
            - $ref: '#/components/schemas/NetWorthAmount'
            - type: 'null'
        share:
          $ref: '#/components/schemas/NetWorthShare'
    NetWorth:
      type: object
      additionalProperties: false
      required:
        - asOf
        - total
        - reason
        - quality
        - stalestAgeDays
        - eligibleAccountCount
        - valuedAccountCount
        - missingValuationCount
        - staleValuationCount
        - delta
        - contributions
        - allocation
      properties:
        asOf:
          type: string
          format: date
        total:
          description: Null with a reason when the sum cannot be produced. Never `0`.
          oneOf:
            - $ref: '#/components/schemas/NetWorthAmount'
            - type: 'null'
        reason:
          oneOf:
            - $ref: '#/components/schemas/NetWorthReason'
            - type: 'null'
        quality:
          $ref: '#/components/schemas/ValuationQuality'
        stalestAgeDays:
          description: Age in days of the oldest valuation behind the figure.
          oneOf:
            - type: integer
              minimum: 0
            - type: 'null'
        eligibleAccountCount:
          type: integer
          minimum: 0
        valuedAccountCount:
          type: integer
          minimum: 0
        missingValuationCount:
          type: integer
          minimum: 0
        staleValuationCount:
          type: integer
          minimum: 0
        delta:
          $ref: '#/components/schemas/NetWorthDelta'
        contributions:
          type: array
          items:
            $ref: '#/components/schemas/NetWorthContribution'
        allocation:
          type: array
          items:
            $ref: '#/components/schemas/NetWorthAllocationEntry'
    NetWorthPoint:
      type: object
      additionalProperties: false
      required:
        - on
        - total
        - reason
        - quality
      properties:
        on:
          type: string
          format: date
        total:
          oneOf:
            - $ref: '#/components/schemas/NetWorthAmount'
            - type: 'null'
        reason:
          oneOf:
            - $ref: '#/components/schemas/NetWorthReason'
            - type: 'null'
        quality:
          $ref: '#/components/schemas/ValuationQuality'
    NetWorthHistory:
      type: object
      additionalProperties: false
      required:
        - asOf
        - granularity
        - points
      properties:
        asOf:
          type: string
          format: date
        granularity:
          type: string
          enum:
            - MONTH
        points:
          description: Oldest first. The last point is the requested day itself.
          type: array
          items:
            $ref: '#/components/schemas/NetWorthPoint'
    TransactionPage:
      type: object
      additionalProperties: false
      required:
        - items
        - nextCursor
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Transaction'
        nextCursor:
          description: Cursor for the following page, or null on the last page.
          oneOf:
            - type: string
            - type: 'null'
    Transaction:
      type: object
      additionalProperties: false
      required:
        - id
        - accountId
        - amount
        - originalAmount
        - exchangeRate
        - nature
        - state
        - source
        - bookedOn
        - valueOn
        - authorizedOn
        - rawLabel
        - counterparty
        - note
        - paymentMethod
        - mcc
        - maskedCard
        - bankReference
        - splits
        - version
        - createdAt
        - updatedAt
        - voidedAt
        - transferId
      properties:
        id:
          type: string
          format: uuid
        accountId:
          type: string
          format: uuid
        amount:
          $ref: '#/components/schemas/DecimalAmount'
        originalAmount:
          description: Cross-asset figure carried beside the booked amount, or null.
          oneOf:
            - $ref: '#/components/schemas/DecimalAmount'
            - type: 'null'
        exchangeRate:
          oneOf:
            - type: string
              pattern: '^(?!-0(?:\.0+)?$)-?(?:0|[1-9][0-9]*)(?:\.[0-9]+)?$'
            - type: 'null'
        nature:
          $ref: '#/components/schemas/TransactionNature'
        state:
          $ref: '#/components/schemas/TransactionState'
        source:
          $ref: '#/components/schemas/TransactionSource'
        bookedOn:
          type: string
          format: date
        valueOn:
          oneOf:
            - type: string
              format: date
            - type: 'null'
        authorizedOn:
          oneOf:
            - type: string
              format: date
            - type: 'null'
        rawLabel:
          type: string
          minLength: 1
          maxLength: 140
          description: >-
            Original source wording. It can be corrected only for a manually entered
            transaction.
        counterparty:
          oneOf:
            - type: string
              minLength: 1
              maxLength: 80
            - type: 'null'
        note:
          oneOf:
            - type: string
              minLength: 1
              maxLength: 500
            - type: 'null'
        paymentMethod:
          oneOf:
            - $ref: '#/components/schemas/PaymentMethod'
            - type: 'null'
        mcc:
          oneOf:
            - type: string
              pattern: '^[0-9]{4}$'
            - type: 'null'
        maskedCard:
          oneOf:
            - type: string
              pattern: '^[0-9]{4}$'
            - type: 'null'
        bankReference:
          oneOf:
            - type: string
              minLength: 1
              maxLength: 64
            - type: 'null'
        splits:
          type: array
          maxItems: 20
          items:
            $ref: '#/components/schemas/TransactionSplit'
        version:
          type: integer
          minimum: 1
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        voidedAt:
          oneOf:
            - type: string
              format: date-time
            - type: 'null'
        transferId:
          description: >-
            Read-only. Set when this transaction is a transfer leg or its
            fee; editing or voiding it then requires the transfer's own
            endpoint instead of this one.
          oneOf:
            - type: string
              format: uuid
            - type: 'null'
    TransactionSplit:
      type: object
      additionalProperties: false
      required:
        - id
        - categoryId
        - categoryLabel
        - categoryIcon
        - categoryColor
        - amount
        - analyticAxes
        - note
      properties:
        id:
          type: string
          format: uuid
        categoryId:
          type: string
          format: uuid
        categoryLabel:
          type: string
          minLength: 1
          maxLength: 80
        categoryIcon:
          description: >-
            Read-only display icon of the split category. Never accepted on write.
          oneOf:
            - type: string
              pattern: '^[a-z][a-z0-9-]{0,31}$'
            - type: 'null'
        categoryColor:
          description: >-
            Read-only display colour of the split category. Never accepted on write.
          oneOf:
            - type: string
              pattern: '^#[0-9A-F]{6}$'
            - type: 'null'
        amount:
          $ref: '#/components/schemas/DecimalAmount'
        analyticAxes:
          $ref: '#/components/schemas/AnalyticAxes'
        note:
          oneOf:
            - type: string
              minLength: 1
              maxLength: 140
            - type: 'null'
    TransactionNature:
      type: string
      enum:
        - INCOME
        - EXPENSE
        - TRANSFER
        - REFUND
        - FEE
        - ADJUSTMENT
    StandaloneTransactionNature:
      type: string
      enum:
        - INCOME
        - EXPENSE
        - FEE
        - ADJUSTMENT
    TransactionState:
      type: string
      enum:
        - PENDING
        - BOOKED
        - VOIDED
        - REJECTED
    TransactionSource:
      type: string
      enum:
        - MANUAL
        - IMPORT
        - PROVIDER
    PaymentMethod:
      type: string
      enum:
        - CARD
        - TRANSFER
        - DIRECT_DEBIT
        - CHECK
        - CASH
        - OTHER
    CreateTransactionRequest:
      type: object
      additionalProperties: false
      required:
        - accountId
        - amount
        - nature
        - state
        - bookedOn
        - valueOn
        - authorizedOn
        - rawLabel
        - counterparty
        - note
        - paymentMethod
        - mcc
        - maskedCard
        - bankReference
        - categoryId
        - splits
      properties:
        accountId:
          type: string
          format: uuid
        amount:
          $ref: '#/components/schemas/DecimalAmount'
        nature:
          $ref: '#/components/schemas/StandaloneTransactionNature'
        state:
          type: string
          enum:
            - PENDING
            - BOOKED
        bookedOn:
          type: string
          format: date
        valueOn:
          oneOf:
            - type: string
              format: date
            - type: 'null'
        authorizedOn:
          oneOf:
            - type: string
              format: date
            - type: 'null'
        rawLabel:
          type: string
          minLength: 1
          maxLength: 140
        counterparty:
          oneOf:
            - type: string
              minLength: 1
              maxLength: 80
            - type: 'null'
        note:
          oneOf:
            - type: string
              minLength: 1
              maxLength: 500
            - type: 'null'
        paymentMethod:
          oneOf:
            - $ref: '#/components/schemas/PaymentMethod'
            - type: 'null'
        mcc:
          oneOf:
            - type: string
              pattern: '^[0-9]{4}$'
            - type: 'null'
        maskedCard:
          oneOf:
            - type: string
              pattern: '^[0-9]{4}$'
            - type: 'null'
        bankReference:
          oneOf:
            - type: string
              minLength: 1
              maxLength: 64
            - type: 'null'
        categoryId:
          oneOf:
            - type: string
              format: uuid
            - type: 'null'
        splits:
          description: >-
            An explicit multi-row allocation, replacing the categoryId
            shorthand. Null keeps the shorthand; an empty list is a
            deliberate, uncategorised transaction.
          oneOf:
            - type: array
              maxItems: 20
              items:
                $ref: '#/components/schemas/TransactionSplitInput'
            - type: 'null'
    UpdateTransactionRequest:
      type: object
      additionalProperties: false
      required:
        - accountId
        - amount
        - nature
        - state
        - bookedOn
        - valueOn
        - authorizedOn
        - rawLabel
        - counterparty
        - note
        - paymentMethod
        - mcc
        - maskedCard
        - bankReference
        - categoryId
        - splits
        - version
      properties:
        accountId:
          type: string
          format: uuid
        amount:
          $ref: '#/components/schemas/DecimalAmount'
        nature:
          $ref: '#/components/schemas/StandaloneTransactionNature'
        state:
          type: string
          enum:
            - PENDING
            - BOOKED
            - REJECTED
        bookedOn:
          type: string
          format: date
        valueOn:
          oneOf:
            - type: string
              format: date
            - type: 'null'
        authorizedOn:
          oneOf:
            - type: string
              format: date
            - type: 'null'
        rawLabel:
          type: string
          minLength: 1
          maxLength: 140
          description: >-
            Editable for manually entered transactions. Imported or provider-sourced
            transactions must echo their original source label unchanged.
        counterparty:
          oneOf:
            - type: string
              minLength: 1
              maxLength: 80
            - type: 'null'
        note:
          oneOf:
            - type: string
              minLength: 1
              maxLength: 500
            - type: 'null'
        paymentMethod:
          oneOf:
            - $ref: '#/components/schemas/PaymentMethod'
            - type: 'null'
        mcc:
          oneOf:
            - type: string
              pattern: '^[0-9]{4}$'
            - type: 'null'
        maskedCard:
          oneOf:
            - type: string
              pattern: '^[0-9]{4}$'
            - type: 'null'
        bankReference:
          oneOf:
            - type: string
              minLength: 1
              maxLength: 64
            - type: 'null'
        categoryId:
          oneOf:
            - type: string
              format: uuid
            - type: 'null'
        splits:
          description: >-
            An explicit multi-row allocation, replacing the categoryId
            shorthand. Null keeps the shorthand; an empty list clears the
            allocation and returns the transaction to the "to categorise"
            queue.
          oneOf:
            - type: array
              maxItems: 20
              items:
                $ref: '#/components/schemas/TransactionSplitInput'
            - type: 'null'
        version:
          type: integer
          minimum: 1
    ReplaceTransactionSplitsRequest:
      type: object
      additionalProperties: false
      required:
        - splits
        - version
      properties:
        splits:
          type: array
          maxItems: 20
          items:
            $ref: '#/components/schemas/TransactionSplitInput'
        version:
          type: integer
          minimum: 1
    TransactionSplitInput:
      type: object
      additionalProperties: false
      required:
        - categoryId
        - amount
        - analyticAxes
        - note
      properties:
        categoryId:
          type: string
          format: uuid
        amount:
          $ref: '#/components/schemas/DecimalAmount'
        analyticAxes:
          description: >-
            Null inherits the picked category's default axes; an explicit
            list, even empty, overrides them. An override never rewrites the
            category itself.
          oneOf:
            - $ref: '#/components/schemas/AnalyticAxes'
            - type: 'null'
        note:
          oneOf:
            - type: string
              minLength: 1
              maxLength: 140
            - type: 'null'
    VoidTransactionRequest:
      type: object
      additionalProperties: false
      required:
        - version
      properties:
        version:
          type: integer
          minimum: 1
    Transfer:
      type: object
      additionalProperties: false
      required:
        - id
        - source
        - target
        - fee
        - exchangeRate
        - version
        - createdAt
        - updatedAt
        - voidedAt
      properties:
        id:
          type: string
          format: uuid
        source:
          $ref: '#/components/schemas/Transaction'
        target:
          $ref: '#/components/schemas/Transaction'
        fee:
          description: The separate FEE transaction on the source account, or null.
          oneOf:
            - $ref: '#/components/schemas/Transaction'
            - type: 'null'
        exchangeRate:
          description: Derived rate for a cross-asset transfer, or null for a same-asset one.
          oneOf:
            - type: string
              pattern: '^(?!-0(?:\.0+)?$)-?(?:0|[1-9][0-9]*)(?:\.[0-9]+)?$'
            - type: 'null'
        version:
          type: integer
          minimum: 1
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        voidedAt:
          oneOf:
            - type: string
              format: date-time
            - type: 'null'
    CreateTransferRequest:
      type: object
      additionalProperties: false
      required:
        - sourceAccountId
        - targetAccountId
        - sourceAmount
        - targetAmount
        - state
        - bookedOn
        - valueOn
        - label
        - note
        - fee
      properties:
        sourceAccountId:
          type: string
          format: uuid
        targetAccountId:
          type: string
          format: uuid
        sourceAmount:
          description: A positive magnitude; the server negates it for the source leg.
          $ref: '#/components/schemas/DecimalAmount'
        targetAmount:
          description: >-
            A positive magnitude in the target account asset. Required when
            the two accounts do not share an asset; optional otherwise,
            where it defaults to the source magnitude.
          oneOf:
            - $ref: '#/components/schemas/DecimalAmount'
            - type: 'null'
        state:
          type: string
          enum:
            - PENDING
            - BOOKED
        bookedOn:
          type: string
          format: date
        valueOn:
          oneOf:
            - type: string
              format: date
            - type: 'null'
        label:
          type: string
          minLength: 1
          maxLength: 140
          description: Becomes the raw label of both legs and the fee, unsuffixed in storage.
        note:
          oneOf:
            - type: string
              minLength: 1
              maxLength: 500
            - type: 'null'
        fee:
          description: >-
            A negative amount in the source account asset, producing a
            separate FEE transaction on the source account. Null when there
            is no fee.
          oneOf:
            - $ref: '#/components/schemas/DecimalAmount'
            - type: 'null'
    UpdateTransferRequest:
      type: object
      additionalProperties: false
      required:
        - sourceAccountId
        - targetAccountId
        - state
        - bookedOn
        - valueOn
        - label
        - note
        - fee
        - version
      properties:
        sourceAccountId:
          type: string
          format: uuid
        targetAccountId:
          type: string
          format: uuid
        state:
          type: string
          enum:
            - PENDING
            - BOOKED
        bookedOn:
          type: string
          format: date
        valueOn:
          oneOf:
            - type: string
              format: date
            - type: 'null'
        label:
          type: string
          minLength: 1
          maxLength: 140
        note:
          oneOf:
            - type: string
              minLength: 1
              maxLength: 500
            - type: 'null'
        fee:
          description: Replaces, adds or removes the fee. Null means no fee.
          oneOf:
            - $ref: '#/components/schemas/DecimalAmount'
            - type: 'null'
        version:
          type: integer
          minimum: 1
    VoidTransferRequest:
      type: object
      additionalProperties: false
      required:
        - version
      properties:
        version:
          type: integer
          minimum: 1
    DecimalAmount:
      description: >-
        A financial decimal. Every amount, quantity, price and rate travels as
        a canonical decimal string paired with its asset code; a native JSON
        number is forbidden, because parsing one would put a binary float back
        into a financial value.
      type: object
      additionalProperties: false
      required:
        - value
        - assetCode
      properties:
        value:
          type: string
          pattern: '^(?!-0(?:\.0+)?$)-?(?:0|[1-9][0-9]*)(?:\.[0-9]+)?$'
          examples:
            - '230.5688'
        assetCode:
          type: string
          pattern: '^[A-Z][A-Z0-9]{1,11}$'
          examples:
            - EUR
    Problem:
      type: object
      additionalProperties: false
      required:
        - type
        - title
        - status
        - detail
      properties:
        type:
          type: string
          format: uri-reference
        title:
          type: string
        status:
          type: integer
          minimum: 400
          maximum: 599
        detail:
          type: string
    AmountInputProblem:
      description: >-
        An RFC 9457 problem raised by the shared exact-amount parser. The JSON
        pointer names the public request member that failed validation.
      type: object
      additionalProperties: false
      required:
        - type
        - title
        - status
        - detail
        - pointer
      properties:
        type:
          type: string
          format: uri-reference
          pattern: '^/problems/amount\.'
        title:
          type: string
        status:
          type: integer
          const: 422
        detail:
          type: string
        pointer:
          type: string
          pattern: '^/'
    TransactionBelongsToTransferProblem:
      description: >-
        Raised by the transaction update, void and split-replacement
        endpoints when the targeted transaction is a transfer leg or its
        fee: only the transfer's own endpoint may change it.
      type: object
      additionalProperties: false
      required:
        - type
        - title
        - status
        - detail
        - transferId
      properties:
        type:
          type: string
          format: uri-reference
          const: /problems/transaction-belongs-to-transfer
        title:
          type: string
        status:
          type: integer
          const: 422
        detail:
          type: string
        transferId:
          description: The transfer to use instead.
          type: string
          format: uuid
